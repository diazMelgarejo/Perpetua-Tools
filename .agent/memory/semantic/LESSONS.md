Warning: truncated output (original token count: 112189)
Total output lines: 1067

- Self-reflection 2026-07-23: Hermes learned that probing existing tool wiring before installing LM Studio shims, sourcing partner CLI paths in start.ps1, and respecting repo pre-push Phase 0 doctrine prevents rework and keeps windows/mac/lan comms valid.  <!-- status=accepted confidence=0.9 evidence=2 id=lesson_retro_20260723 -->

### 2026-06

## Auto-promoted entries will be appended below

### 2026-08

- ~~Handwaving a known error always costs more later than fixing it now. When a check surfaces something that looks minor, tangential, or "not what I'm here to fix," the instinct to note it and move on is usually wrong -- the cost doesn't disappear, it just relocates to whoever hits it next (often the same agent, in a later, more expensive context). Treat every surfaced finding as either fixed now or explicitly, visibly deferred with a stated reason -- never silently absorbed into "I'll get to it."~~  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_0b3f101508e4 superseded_by=lesson_502211a1be56 -->
- ~~Append-only historical records (lessons, audits, vulnerability memory, review ledgers, and any file whose value depends on an intact record of what was actually claimed and when) must never be rewritten in place, even to fix a real error in the original entry. A direct edit destroys the original claim with no queryable trace within the data itself of what it used to say -- exactly the auditability the record exists to provide. The correct fix: append a new entry that supersedes the original (a supersedes/superseded_by back-reference), never mutate the original's fields. This applies to the record itself, not to copies rendered or cached from it -- a machine-generated markdown file regenerated from a corrected append-only source is fine to regenerate in place, and non-historical documents (working docs, prose restating a claim) can be edited directly.~~  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_8ebfd5447e38 superseded_by=lesson_e773f6f957c2 -->
- PR body updates are append-only — ManagePullRequest update_pr and gh pr edit replace the entire body. After every commit on a branch with an open PR, run bash scripts/git/remind-pr-body-append-only.sh before push; use scripts/cursor/append-pr-body.sh for writes. Documented clobber incidents: PT#154, orama#222, PT#298, orama#239, PT#314; treat ~5x undocumented silent clobbers as likely. PR_BODY_GUARD_STRICT=1 blocks publish until PR_BODY_UPDATE_ACK=1.  <!-- status=accepted confidence=0.9 evidence=1 id=lesson_a8f3c2e91d04 -->
- When resolving real overlap between a new draft and an existing, more comprehensive doctrine, "avoid duplication" does not automatically mean "make the newer, simpler thing subordinate to the older, more complex one." It means determining which document actually serves which situation and sizing each to its own job. A heavyweight protocol built for a rare, hard problem (e.g. concurrent multi-agent edits to the same repo, or reconciling a fork separated from its source by months of drift) should not become the default an agent reads first for the common, simple case (one branch, one agent, no concurrent editing) just because it existed first and is more thorough. Getting this backwards is not caught by re-reading a project's own stated single-source-of-truth value, since that value is genuinely being honored in some sense (no literal duplication) even while the sizing is wrong -- it may require direct correction from a human collaborator who can see the actual audience mismatch.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_718c9430f44b -->
- Several tooling and API gotchas recur across a working session even after being individually caught once, because a single encounter doesn't automatically generalize into a standing rule: the PR-list API's merged field being unreliable (must check the single-PR endpoint); squash-merged branches never showing as git ancestors (verify by content/ID presence, not ancestry); a stale local checkout after an earlier push in the same session diverging silently; and diff-scoped CI lint checking a whole touched file, not just the changed hunk. Each of these needs to be treated as a standing checklist item to consult before the relevant operation, not a one-off lesson learned and then re-derived from scratch the next time it's encountered.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_2d53056e593c -->
- When syncing attribution guards across PT, orama, and AlphaClaw, fix PT on its single open guard PR first, then byte-copy only GUARD_SYNC manifest paths to the one open PR per downstream repo — never open fragmented one-file PRs and never run orama-to-PT sync until PT canonical merges, or stale orama copies overwrite PT improvements.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_2f59523a02e3 -->
- Before running sync-attribution-guard-scripts.sh, run check-guard-sync-divergence.sh --workspace; abort when any sibling guard file differs from orama HEAD and the sibling blob is absent from orama history — that means the sibling is ahead and blind sync would clobber improvements. Promote sibling to orama on the single open PR per repo, then sync downstream.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_200af7108094 -->
- Layer 0 for Cursor agents: never automatically change an existing PR description — use ManagePullRequest post_comment or gh pr comment only; hooks block update_pr with body=, gh pr edit, and append-pr-body.sh  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_696cf70a34bd -->
- ~~When a human explicitly authorizes a PR body edit, set CURSOR_PR_BODY_HUMAN_OVERRIDE_ACK=1 then follow append-only Layers 1-6 (READ backup MERGE full body via append-pr-body.sh); delta-only writes remain forbidden~~  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_4c914783ec46 superseded_by=lesson_d1f3789d4592 -->
- Guard-sync waves use one open PR per repo (PT then orama then AlphaClaw); run check-guard-sync-divergence.sh --workspace before sync-attribution-guard-scripts.sh; never open parallel one-file guard PRs  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_e8ae3f3b9fa6 -->
- Cloud turn-end update PR must mean post_comment not update_pr body — treating description field as a scratch pad erases Summary template sections and CodeRabbit tails  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_c9ff6cc2625d -->
- When markdownlint-cli2 v0.17.2 lints explicit file globs in CI, root .markdownlint.json filter overrides do not apply to catalog SKILL.md files outside the skills tree — add a per-directory .markdownlint-cli2.jsonc (e.g. line_length 500 for bin/orama-system/) and delete conflicting .markdownlint.json.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_64e972d330c2 -->
- Guard-sync divergence checks must discover workspace siblings via git -C path rev-parse --show-toplevel (not test -d path/.git), reject surplus CLI arguments with exit 2, fail-closed when check-guard-sync-divergence.sh is missing, and scan the full outgoing pre-push commit range for scripts/git/ touches.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_82c94865243e -->
- PR body Layer 0: Cursor agents comment only (post_comment or gh pr comment). Body writes require an operator TTY grant via grant-pr-body-human-override.sh; only append-pr-body.sh is allowed after grant — never update_pr body=, gh pr edit, gh api body mutations, or agent-forgeable env exports.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_8c5f6349aa87 -->
- PR-body operator grant v2 replay state machine: append-pr-body.sh must reserve nonce before gh pr edit, mark-applied only after remote success, consume last; release reservation on edit failure; reconcile CLI consumes when follow-up block already on remote (crash recovery). Never consume before gh pr edit succeeds.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_cd51f7e6bf6c -->
- operator-grant-v2 HMAC canonical payload is fixed-order UTF-8 bytes grant-v2|repo|pr|nonce|issued_at|action|content_digest with no trailing newline; mint and verify must use identical bytes; golden vector test locks regression.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_720dedfdd2c8 -->
- Same-user Keychain HMAC for PR-body grants is escalation control not cryptographic human identity — agent shells with PTY can read the secret or ack file; MVP raises effort; WebAuthn deferred to security-sentinel v2.1.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_446162929cb3 -->
- Operator-gated PR body edits require operator-grant-v2 HMAC ack from grant-pr-body-human-override.sh matching append bytes -- not CURSOR_PR_BODY_HUMAN_OVERRIDE_ACK env exports or operator-grant-v1 plaintext ack. The HMAC grant authorizes the append-pr-body.sh capability itself; it does not prove human identity or presence -- the underlying grant script's TTY-presence check is not a reliable human-vs-agent signal.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_d1f3789d4592 -->
- Tracked .agent memory and working chronicles must not embed ephemeral scratch path literals — repo_hygiene flags them like workstation home paths and blocks PT CI.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_1a6dc6bccad0 -->
- Human-authorized PR body edits require operator-grant-v2 HMAC ack from grant-pr-body-human-override.sh matching append bytes — not CURSOR_PR_BODY_HUMAN_OVERRIDE_ACK env exports or operator-grant-v1 plaintext ack.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_df758c2d1245 -->
- When a markdownlint (or similar whole-file linter) violation surfaces in a file pulled into scope by an unrelated edit, the correct response depends on the file's nature, not a blanket policy: machine-rendered, append-only historical records (a lessons/decisions log where entries are added mechanically over time, meant to be terse and grow indefinitely) should be excluded from the specific rule that doesn't meaningfully apply (e.g. line-length on a log that append-only tooling writes one dense entry per line), via a scoped config override -- not hand-reformatted, since doing so fights the tool that generates the file. Genuinely hand-authored prose (a narrative doc, a plan, a README) should have violations fixed properly, even if the count is nontrivial, since the rule is actually serving its purpose there. Determine which kind of file it is by reading its actual content and structure (does it read as authored narrative, or as accumulated terse entries with a consistent machine-written shape) before choosing exclude vs. fix, not by file location or extension alone.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_a791e2ee5ad6 -->
- When a code review lists multiple findings, checking each one against the actual current file content before fixing it is not a formality -- in a real remediation pass, roughly half of a review's findings can already be correctly handled in current code (fixed by an earlier commit, or never actually present as described), and treating every finding as needing action wastes effort and risks introducing unnecessary or even incorrect changes to code that was already right. The check itself is cheap (grep or read the exact lines cited); skipping it in favor of blindly applying every suggested diff is the expensive mistake.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_9cc7ae1a5f12 -->
- A security guard that inspects shell commands by splitting on command separators (&&, ;, ||) to check each resulting segment independently must also split on bare newlines -- a shell interprets a newline as a command separator too, and a guard that omits it leaves an exploitable gap: a legitimate-looking command on the first line can cause the guard to short-circuit to ALLOW, leaving a malicious second command hidden on the next line completely uninspected. When testing such a guard's segment-splitting logic, the test must exercise the actual bypass condition (the legitimate first command's branch must be genuinely capable of returning ALLOW, e.g. via a properly-mocked active authorization state) -- a test where the first segment already denies on its own passes regardless of whether the fix is present, proving nothing about the real vulnerability.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_4c65d5956822 -->
- A conditional guard like `if [ -f "$script" ] && command -v "$interpreter" >/dev/null; then run "$script"; else run_fallback; fi` silently downgrades security coverage whenever the interpreter is missing but the primary script exists -- it falls through to a narrower fallback path without any signal that the stronger check was skipped. The fallback branch should be reserved for the case the comment actually describes (the primary script itself being absent, e.g. an old clone that predates it); a present-but-unrunnable primary script should fail loudly and closed, not silently downgrade to a weaker check.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_5fb495abcfbc -->
- When a config fix for a CI tool (linter, scanner, formatter) is verified locally before pushing, verifying against a locally-installed "latest" or default version of that tool is not sufficient -- CI workflows commonly pin a specific action/tool version (e.g. a GitHub Action tagged @v19) that can be substantially older than whatever installs by default locally, and newer tool versions can silently support config schema or CLI flags that the older, actually-pinned version does not. A config change that tests clean locally can still fail in real CI for this reason alone. Before trusting a local verification of a CI-tool config fix, identify the exact tool version the CI workflow actually pins (check the action's own release notes or its own bundled dependency versions, often visible in error message doc-links CI itself produces) and install that exact version locally to test against, not the newest available.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_77ce859d6970 -->
- Secret-scanning tools like gitleaks scan the full commit history range in a branch/PR by default, not just the current tree state -- a test fixture or example value that merely resembles a secret (e.g. a hardcoded hex digest used as an expected HMAC output) can trip a generic entropy detector, and even after a later commit fixes the pattern by removing the literal value, the scanner keeps flagging the earlier, now-superseded commit on every subsequent CI run for that branch, because the flagged content is still present in that commit's diff within history. This is a real, recurring category of false positive distinct from an actual leaked secret, and the correct fix is a scoped allowlist entry for the specific commit SHA(s) in the scanner's config (with `useDefault = true` or equivalent to keep the rest of the default ruleset intact) -- not editing the current file again, since the flagged content is no longer there, and not disabling the scanner or its ruleset broadly.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_084412e4e566 -->
- A short/abbreviated git commit SHA (e.g. from `git log --oneline` output) must never be extended into a full 40-character SHA by guessing, padding, or pattern-completing the remaining hex digits -- even when the extension is meant only for a config file (an allowlist, a pin) rather than executable code, a fabricated SHA silently fails to match anything (a security tool's allowlist entry that doesn't match the real commit provides zero actual exemption, while looking correct at a glance) or, worse, could coincidentally collide with an unrelated real commit. Always resolve a full SHA explicitly with `git rev-parse <short-sha>` and use that exact output, verified by direct comparison, before writing it into any tracked file.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_5f0362a3f95f -->
- When two changes bundled in the same commit are meant to work together (a feature-gating flag and the feature it gates, a deferral/rollout date and the behavior that respects it), verify they are actually consistent with each other by running the specific test that exercises that consistency together -- not just trusting each change's own isolated test coverage. A change that extends or modifies a gate without checking what depends on that gate's current state can silently ship an internal contradiction that only surfaces once something exercises both halves at once.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_61d719978435 -->
- When removing a duplicate call site as a concurrency/correctness fix, check whether that call site actually serves two different purposes depending on which code state it runs in before deleting it everywhere -- the same fallback or retry call can be genuine duplication in one state (e.g. a single-racer scenario where the fallback target is already the racer that just failed) and the intended, necessary behavior in a different state (e.g. a multi-racer scenario where the fallback target was never itself one of the racers). Removing it unconditionally fixes the duplication bug in the first state while introducing a regression in the second; the correct fix is usually conditional on which state actually applies, not a blanket removal.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_64d1deca2fca -->
- Confirming a large batch of test failures is pre-existing and unrelated to a session's own changes (via a before/after comparison) is a necessary check but not the same as understanding why the failures exist -- stopping there leaves a large, alarming-looking failure count in the record with no actual explanation. Where time allows, finishing the diagnosis by installing missing dependencies one at a time and re-running (watching the count drop) can turn "pre-existing, unrelated, unexplained" into "pre-existing, unrelated, and here is exactly why, with zero project-level fix needed" -- a materially more useful record for whoever encounters the same large failure count next and would otherwise have to re-investigate it from scratch.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_8bf6b00d2a44 -->
- For the periscope repo, ALWAYS run Go tests with 'CGO_ENABLED=1 go test -tags fts5 ./...' (per the repo's own Makefile and .github/workflows/ci.yml -- every real invocation uses -tags fts5, several add more tags like pgtest/s3test/evalingest for specific suites). Never run plain 'go test ./...' there -- it silently disables the FTS5 sqlite module (mattn/go-sqlite3 needs the build tag to compile in fts5 support), producing spurious 'no such table: messages_fts' / phase-message-ordering failures that look like real bugs but are just a missing build tag.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_3fe9ebcaf9e0 -->
- ~~An agent renaming a directory/binary must never also infer a broader identity change (module path, package namespace, org identity) in the same commit unless explicitly authorized — each identity-bearing change needs its own explicit human sign-off, even when bundled with an authorized structural rename~~  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_0c339d39a80c superseded_by=lesson_d7f233fdd47e -->
- Never judge a config value 'stale' or 'correct' relative to an upstream/grandmother repo from memory or from your own fork's history alone — fetch the live upstream file (raw.githubusercontent.com or equivalent) directly before asserting what it currently contains  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_aedbe2d7db06 -->
- Undetected agentic scope-creep can be fully recovered days later without a history rewrite: trace the exact origin commit via git log --follow/blame, verify the true baseline against live upstream sources, then land a mechanical forward revert on a fresh branch with full compile and test reverification -- never force-push or rebase through already-shared history to 'undo' it  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_7620f39c87bf -->
- Hermes dispatch has three lanes: L-H1 native delegate_task children, L-PT PT spawn_hermes_agent scripts, L-Fleet coord_pulse cursor-agent jobs — never conflate in skill prose or REGISTRY staging  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_2ecbe4c7eaa6 -->
- hermes-delegate and hermes_harness.py are L-PT parallel AIAgent threads — NOT Nous delegate_task and NOT Hermes-native subagent children  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_1fda0fc21d52 -->
- Windows coord_pulse dispatches cursor-agent via win_job_queue — not Hermes delegate_task; subagent/win-* git branches are L-Fleet file-inbox coordination fiction  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_23b2a4dce4a8 -->
- bin/agents REGISTRY.yml maps OpenClaw pipeline stages to Hermes profile staging — not a live runtime subagent tree  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_0343a2bd15b2 -->
- Scheduled coord_pulse on Windows does not load .env.local — ORAMA_SYSTEM_PATH and PERPETUA_TOOLS_PATH must be User-level env vars or explicit exports  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_5139fb44b427 -->
- Do not run sync-gbrain code stage while gbrain autopilot is active — orchestrator refuses destructive source ops; disable autopilot LaunchAgent first when lock or stale index  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_c70ab2e5a60e -->
- OpenClaw to Hermes graft must run Wave 0 taxonomy and lane tags before Wave 1 JSON envelope — SKIP recursive-spawn-protocol into hermes-delegate until rename  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_c9fb1689c1aa -->
- Activating a provider config in a tool's own settings store (e.g. Cline's providers.json) does not make the CLI use it at runtime -- check for a separate top-level provider-selection flag/default before assuming a config write took effect  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_49e8701ba9e9 -->
- A vendor-doc's exact sample curl/Python for a third-party OpenAI-compatible API is not guaranteed correct -- an optional-looking parameter (an OpenAI-Project header, or the SDK's project= kwarg) can silently break auth (401) even though the underlying key is valid; test with and without each optional param before trusting the documented sample verbatim  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_3c85823ef46f -->
- When extracting or inspecting a secret from a config file via a shell one-liner, never use a bare grep/cat on the raw file -- pipe through a redaction step or use a script that only reports length/presence, even when the extraction command's PRIMARY purpose looks safe (e.g. checking a var name exists)  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_0c78b132c55b -->
- GNU coreutils flock(1) (the standalone command) is not present on macOS/BSD by default -- confirmed missing even on a fully-set-up dev machine, not just a fresh install. Never gate a bash script's mutual-exclusion on the flock command; use Python's fcntl.flock() instead (a real POSIX syscall wrapper available in Python's stdlib on Darwin) if the script already shells out to python3, or the portable mkdir-is-atomic lock idiom if it's pure bash.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_a10088cab4ae -->
- Second-precision timestamps used as unique filenames (e.g. for rotating backup/version history) will silently collide under any workload faster than 1 call per second -- including two successive calls from the same automated script or an agent iterating quickly. Always use microsecond (or finer) precision, a monotonic counter, or a uuid suffix for filename-based versioning, never bare %Y%m%dT%H%M%SZ.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_7ef854a99c2d -->
- A plan/audit document's own 'Done' status table is not proof the work is actually done for every row -- verify each claimed-complete item against the real filesystem/content individually, the same rigor as verifying an external upstream claim, just scoped internally to this repo's own docs.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_d232d1a1d91d -->
- For any tool that hot-swaps between named configs carrying live credentials (API keys, provider profiles), never write directly into the live config file inline -- always run through: (1) a checked-in *.tmpl with ${VAR} placeholders holding zero real secrets, (2) a resolver that fills placeholders from a canonical secrets/<name> (0600) + .env.<name> loader pair, (3) an idempotent snapshot of the outgoing state into a rotating, capped backup history stored OUTSIDE the git repo (since a snapshot holds a real resolved secret), before the atomic write. This is now the established convention on this machine for every provider secret set up this session (Firecrawl, GitHub Copilot, Cline's Google/wanDB/BigModel profiles) -- follow it for any new one rather than inventing a new scheme.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_322a2fba2337 -->
- When a concurrent agent commits directly to the same working directory/branch mid-review, verify by reading actual diffs and running the actual test suite before crediting or trusting any of its claimed fixes -- never trust a commit message's claimed test count.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_11baf3e69fcb -->
- Independent dual-voice AI review (Codex + a separate Claude subagent, no shared context) that converges on the identical bug by reading the same code independently is the strongest verification signal available -- weight it above either voice's severity rating alone.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_fb76280fc2cd -->
- The EXA daemon-wrapper pattern (singleton daemon + Unix-socket bridge to avoid N duplicate npx child processes) only applies to locally-spawned stdio MCP servers -- it does not transfer to type:http MCP servers like Firecrawl/GitHub Copilot, since Claude Code connects to those directly via HTTP with no local backend process to daemonize.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_2c4124092607 -->
- An agent renaming a directory/binary must never also infer a broader identity change (module path, package namespace, org identity) in the same commit unless explicitly authorized — each identity-bearing change needs its own explicit human sign-off, even when bundled with an authorized structural rename. When citing live go.mod values after such an incident, verify each repository independently: GitHub repo lineage (wesm/agentsview redirects to kenn-io/agentsview) does not imply a fork already adopted the parent vanity module path.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_d7f233fdd47e -->
- Meta-instruction, in effect until the v2 oramasys org multi-repo centralization is complete: before pushing a fix to either orama-system or Perpetua-Tools, check whether the same file, or the same underlying concept, exists in the sibling repo and whether it's actually in sync -- not just "did I apply the fix I meant to apply," but "is the BEST version of this fix now present in both repos, chosen by comparing them directly, not by assuming whichever repo I fixed first is automatically correct." Where the two repos' versions differ, the better one (more defensive pattern, more complete investigation notes, more accurate glob/scope) should be adopted in both, not just the one being actively worked on. This bidirectional check is a stopgap for the current two-repo-with-manual-sync architecture; once v2's centralized package/multi-repo structure lands, this class of drift should become structurally impossible rather than something that needs checking by hand every time.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_b1cfecbc4eb6 -->
- Meta-instruction, in effect until the v2 oramasys org multi-repo centralization is complete: before pushing a fix to either orama-system or Perpetua-Tools, check whether the same file, or the same underlying concept, exists in the sibling repo and whether it's actually in sync -- not just "did I apply the fix I meant to apply," but "is the BEST version of this fix now present in both repos, chosen by comparing them directly, not by assuming whichever repo I fixed first is automatically correct." Where the two repos' versions differ, the better one (more defensive pattern, more complete investigation notes, more accurate glob/scope) should be adopted in both, not just the one being actively worked on. This bidirectional check is a stopgap for the current two-repo-with-manual-sync architecture; once v2's centralized package/multi-repo structure lands, this class of drift should become structurally impossible rather than something that needs checking by hand every time.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_6ee66e3cd922 -->
- When a local episodic/memory branch reports ahead-of-origin by a small N, identify the exact commits, check reachability from main and the active consolidation branch, and compare patch-id/content before assuming unmerged feature work — local closeout memory often sits only on a stale tip after related PRs already merged.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_1103c662e580 -->
- ~~Cherry-picking append-only JSONL memory (AGENT_LEARNINGS.jsonl) onto a divergent tip: resolve EOF conflicts by union/dedup on source.run_id, append only unique rows using the original line bytes, and never json.dumps-rewrite the whole file (Unicode escape churn creates huge false diffs).~~  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_0f50e7407f77 superseded_by=lesson_aca46de3a5aa -->
- Cherry-picking append-only JSONL memory (AGENT_LEARNINGS.jsonl) onto a divergent tip: resolve EOF conflicts by union/dedup on top-level id or nested source.run_id (first occurrence wins per key), append only unique rows using the original line bytes, and never json.dumps-rewrite the whole file (Unicode escape churn creates huge false diffs).  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_aca46de3a5aa -->
- Append-only historical records must never be rewritten in place, even to fix a real error -- including via checkout --theirs or any other whole-file overwrite during conflict resolution. The correct fix: restore the original record exactly, then append a new entry whose supersedes field names the original lesson id, carrying only the corrected claim text. checkout --theirs looks like a clean conflict resolution but is exactly the in-place-mutation this doctrine bans when applied to an append-only record file, not a regular source file.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_9940e1aa6fc4 -->
- Append-only historical records (lessons, audits, vulnerability memory, review ledgers, and any file whose value depends on an intact record of what was actually claimed and when) must never be rewritten in place, even to fix a real error in the original entry. A direct edit destroys the original claim with no queryable trace within the data itself of what it used to say -- exactly the auditability the record exists to provide. The correct fix: append a new entry that supersedes the original (a supersedes/superseded_by back-reference), never mutate the original's fields. This applies to the record itself, not to copies rendered or cached from it -- a machine-generated markdown file regenerated from a corrected append-only source is fine to regenerate in place, and non-historical documents (working docs, prose restating a claim) can be edited directly.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_071ec367227c -->
- ~~git branch -f <name> <sha> silently no-ops (or unreliably applies) when <name> is the currently checked-out branch -- it does not reliably move HEAD in the same shell invocation. Use git update-ref refs/heads/<name> <sha> instead for a scripted undo-a-cherry-pick loop while staying on the branch; it is a lower-level plumbing command that reliably moves the ref and (since HEAD is a symref to it) is immediately reflected.~~  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_2546180f3d5b superseded_by=lesson_e276758511e6 -->
- ~~git branch -f <name> <sha> refuses to move the currently checked-out branch (exits non-zero; the ref is unchanged). Use git update-ref refs/heads/<name> <sha> to move only the ref while staying on the branch; then synchronize index and working tree afterward (for example git restore --staged --worktree .) so staged/worktree state does not still reflect the undone cherry-pick.~~  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_e276758511e6 superseded_by=lesson_32aea4e6cfff -->
- git restore --staged --worktree . discards ALL unrelated staged and unstaged changes in the working tree, not just the undone cherry-pick's files -- it is a destructive broad-reset disguised as cleanup. Before running it, require a clean git status --short (verifying nothing unrelated is dirty), or scope the restore to only the specific paths the undone commit touched (git restore --staged --worktree -- <path>...). The git update-ref refs/heads/<name> <sha> guidance itself remains correct and unaffected.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_32aea4e6cfff -->
- A subprocess worker pool that only calls proc.kill() on timeout leaves grandchild processes running: kill() signals the direct python -c child only, not any process it spawns itself. Fix: launch each worker with start_new_session=True (own process group), then on timeout send os.killpg(pid, SIGTERM) first (let it clean up), escalate to os.killpg(pid, SIGKILL) if it doesn't exit within a few seconds. Separately, redirect worker stdout/stderr to temp files instead of subprocess.PIPE -- a chatty worker writing more than the OS pipe buffer (~64KB) deadlocks against a poll loop that only drains pipes after the worker exits; files never fill up under the writer, and CLAYGO cleanup (os.remove after reading) keeps them from accumulating.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_92d985d17f3d -->
- An aggregate counter over an append-only JSONL lessons store (e.g. a dashboard's accepted-count) must not do a naive per-line status==accepted count -- retract_lesson.py appends a NEW row with the SAME id and status=retracted rather than editing the original in place, so the original accepted row is still on disk and gets double-counted forever unless the counter dedupes by id (latest row wins). Separately, supersession creates a DIFFERENT id whose supersedes field points at the old one, and the old id's row still literally has status=accepted -- so counters also need to exclude any id referenced by another accepted lesson's supersedes field, mirroring render_lessons.py's _build_auto_section pattern (only ACCEPTED supersessions retire the old lesson; a provisional supersede must not).  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_67383a83bee9 -->
- A repo-wide banned-term grep in a policy doc (e.g. reject unauthorized use of a reserved word like coordinator) false-positives on vendored dependency code that happens to contain the same substring for unrelated reasons -- e.g. a Python virtualenv's bundled pygments lexer listing Apple Cocoa API class names ending in Coordinator is not a policy violation. Fix: scope the check to git ls-files (tracked files only, excluding untracked dependency directories by construction) instead of a plain recursive grep with manually maintained exclude flags -- more robust because it can't be defeated by a new dependency directory the exclude list doesn't yet know about.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_9f3386cb6274 -->
- Even with file-disjoint parallel agent dispatch (each job assigned a distinct set of files), a follow-up direct edit to a file that job is still actively refining can race: reading the file's tail output once and then Edit-ing it later based on that stale read can silently merge on top of content the dispatched job wrote AFTER your read, producing a coherent-looking but unverified final state. The Edit tool's own warning (file changed on disk since last read) is the signal to stop and re-Read fresh before trusting the diff, not just proceed because the edit applied without a hard conflict.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_4e38d15d83ad -->
- When a stacked-PR-family branch's earlier commits have already landed upstream via a different merge (e.g. a sibling PR's squash-merge), git rebase origin/master auto-detects patch-content equivalence and drops the now-redundant commits, printing 'dropping <sha> ... -- patch contents already upstream'; treat that message as confirmation of successful recovery, not an error, and use rebase as the recovery mechanic instead of manual cherry-pick surgery.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_15aa463fd07c -->
- ~~No blanket 'always prefer ours' or 'always prefer theirs' rule is correct for a 3-way merge conflict — read and understand both sides' actual intent and judge case-by-case by which version is the strict quality/robustness superset of the other.~~  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_005f2a16600d superseded_by=lesson_2f377d3a025a -->
- git merge-file -p ours base theirs can literally duplicate an identical textual addition when both 'ours' and 'theirs' independently added the exact same content at slightly different anchor points relative to 'base' -- inspect merge-tool preview output for back-to-back duplicate blocks before promoting or applying it; re-running an apply step that rebuilds the preview directory from scratch wipes any manual de-duplication, so it must be redone if the target sha changes.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_05c055046864 -->
- A merge or blend tool's own 'clean, no conflict' label for a file is not proof nothing was silently dropped -- independently diff base-commit..target-commit for every file the tool marks clean, not just the ones it flags as conflicts, to positively confirm upstream made zero changes there.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_70713965dc1b -->
- When temporarily repointing a git submodule's .gitmodules url at your own fork to consume not-yet-merged upstream PRs, document the temporary state and exact revert path in at least 3 places -- an inline comment in .gitmodules itself, the vendor-tracking doc's current-pin section, and any blend-state or catalog file's note field -- and validate end-to-end via a real fresh-clone plus git submodule update --init, not just the url change alone.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_0f66a36ee1cc -->
- A 'walk this directory and copy or record everything' tool (e.g. a blend-promote script's find $DIR -type f) must explicitly exclude build and bytecode-cache artifacts like __pycache__ compiled files -- .gitignore protects git commits but not tool-level file-walk bookkeeping that reads the raw filesystem directly, so a stray verification command's side-effect files can get silently copied and catalogued as if they were real content.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_76cbf68d38b1 -->
- A GitHub PR's --base must name a branch that exists on the base/target repo, never a fork-only branch -- gh pr create --base <fork-only-branch> against an upstream repo fails with 'Base ref must be a branch' or 'No commits between'; for a family of PRs meant to stack across a fork boundary, base every PR on the upstream default branch (e.g. master) and communicate the dependency in prose (PR body or comments), not via GitHub's native base-branch stacking.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_fcf6fe38c82e -->
- No blanket always-prefer-ours or always-prefer-theirs rule is correct for a 3-way merge conflict — read both sides and prefer the strict quality/robustness superset when one exists; when neither side dominates, combine compatible changes and stop for human review on remaining conflicts so unique valid content is not discarded.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_2f377d3a025a -->
- Never trust a cherry -v + commit-message match as proof a merge/reanchor preserved content — always diff the local tip against the actual merged headRefOid before declaring it safe  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_adda4d2b02c3 -->
- When a CodeRabbit finding cites a language-agnostic coding-style guideline (e.g. '**/*: always create new objects, use immutable update patterns'), verify the pattern has a real non-degrading equivalent in the target language before applying it, rather than mechanically transferring it.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_022af4a9d2b8 -->
- After fixing a process-substitution exit-status blind spot (a producer wrapped in '< <(...)' whose failure a while-read loop can't see), check every remaining stage of that same data flow for the identical gap -- a partial fix that only materializes the FIRST producer to a file can still leave a SECOND filter stage (awk, a second python3 call) wrapped in its own swallowing process substitution.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_189690ff2855 -->
- When a git-worktree cleanup path falls back to plain recursive delete because the proper removal command failed, that fallback does not clear git's internal worktree admin registration -- only the dedicated prune command does; omitting it leaves stale worktree/detached-HEAD metadata that accumulates across every failed removal.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_774d6e8ca9a9 -->
- When cherry-reanchoring a stale branch onto a rewritten/advanced main and hitting an add/add or content conflict, don't resolve by reflexively picking a side -- check whether origin/main's CURRENT full file already contains the branch's content (possibly refined, redacted, or relocated), whether the same lesson/memory IDs already exist on main with equal-or-better text, and for gitlink/submodule conflicts, which SHA is actually ahead. A resolution of 'take origin/main entirely, cherry-pick becomes empty' is a common, CORRECT outcome for old branches, not a failed reconciliation.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_a4a16527bbd1 -->
- During branch reanchor or merge conflict resolution, if one side has a real literal (LAN IP, workstation path, personal email used as an infra detail) and the other side has a placeholder or redacted form of the same value, always keep the redacted side regardless of which side is otherwise 'newer' or 'more detailed' -- a security redaction must never be silently reverted by a content merge.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_1888719f8a52 -->
- The same clustering-before-shipping discipline recurs at five zoom levels and should be applied consistently at all of them: (1) one coherent fix per file, (2) group fixes into commits by theme not touch-order, (3) batch all logical commits locally before a single push, (4) stay on one open branch/PR rather than fragmenting across new ones unless none exists yet, (5) at repo-architecture scale, decide per-function whether it earns its own top-level repo or stays a docs/v2/ subsection of a shared repo -- the same 'does this deserve its own container' judgment, just at a bigger radius.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_442afa34388a -->
- Before any cross-repo guard-sync operation (or when a user asks 'is the other side actually behind, or does it have unique improvements?'), run the divergence checker proactively even when confident nothing has diverged -- treat empirical verification as the mandatory first step, not a defensive measure reserved for when drift is already suspected.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_fa9f7eac575e -->
- check-guard-sync-divergence.sh's WORKSPACE_ROOT defaults to the parent directory of the canonical repo root -- when the canonical repo being pushed from is itself a disposable/scratch clone sitting directly under /tmp (a common pattern for isolating risky git-history-surgery operations), this default silently expands the scan to every unrelated scratch clone under /tmp, producing false-positive GUARD_SYNC_E_DIVERGENCE blocks on totally unrelated pushes. Fix: run the push from a workspace directory that has no other sibling git repos (move/clone the canonical repo into its own isolated parent dir), or verify the flagged 'siblings' are genuinely unrelated scratch work before treating the block as blocking.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_c15cb89de6d4 -->
- Reconciliation decisions (which side of a conflict, divergence, or finding is correct) should be made from what's actually verified to be there, not from which side looks more current by a fast heuristic (recency, version number, 'the other side is usually just behind'). The heuristic and the verified answer usually agree, which is exactly what makes skipping verification feel safe -- the cases where they diverge are silent regressions with no error message anywhere to catch them later.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_8aa89cff9199 -->
- When two independent branches/sessions both attempt to fix the same underlying rule/bug and produce textually different diffs, don't assume they conflict or that one supersedes the other -- install the actual tool (semgrep, in this case) and empirically test both against real positive/negative samples before judging. Two genuinely different-looking fixes can both be correct (same bug, convergent solutions), which is a stronger signal than either fix alone.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_31a635516ea1 -->
- semgrep Bash language support (marked experimental by semgrep's own blog) does not reliably expose quoted strings as a distinct string-literal AST node -- a bare metavariable captures a quoted value INCLUDING its surrounding quotes as part of a generic expression match, while the literal-metavariable quoted syntax (meant to match string literals and strip the quotes) silently matches nothing at all against real bash string assignments. Before trusting any bash-language semgrep or opengrep rule that uses that literal-metavariable quoted syntax or a nested metavariable-pattern block without an explicit generic-language override, test it empirically against real sample code -- a rule that looks syntactically correct can be a silent no-op.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_e8d8ee140589 -->
- When a bash script pipes an external tool through '2>/dev/null || true' inside a scan loop, a missing binary (not just a missing match) silently produces zero hits — always guard with 'command -v <tool>' before the loop, and never trust an environment-isolation fix as root cause until CI itself confirms it against a captured execution trace  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_90fcf80b1355 -->
- For claude.ai web/mobile agents working in an ephemeral bash sandbox (not a persistent local Claude Code CLI session): the sandbox filesystem can be wiped between turns without warning, mid-task, losing all uncommitted local work regardless of how much testing/verification was already done on it. This is not the same failure mode as a lost conversation -- the chat context and prior turns' summaries survive, but the actual git checkouts, installed tools (ripgrep, jq, etc.), and any uncommitted changes do not. Mitigation: commit AND push each logical, independently-tested unit of work as soon as it is complete and verified, rather than accumulating multiple fixes locally and pushing once at the end of a large task -- "push once at the end" is only safe advice for a session that won't lose its filesystem. When a reset is detected (home directory nearly empty, repo checkouts missing), re-clone fresh and re-verify everything from scratch rather than trusting the prior turn's summary of what was done -- installed tooling, test collection, and even file contents need re-confirming, not just re-stated. Re-use the same branch name across the pre-reset and post-reset work (on all affected repos, paired by name if the task spans repos) so recovery doesn't fragment one logical change into multiple disconnected branches. Do not relax RED-first TDD discipline during recovery to save time; re-verify each fix's test genuinely fails before the implementation exists, the same as before the reset -- speed pressure from lost work is not a reason to skip verification.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_df682761347f -->
- Perpetua-Tools contains TS/JS packages (packages/alphaclaw-adapter/, packages/alphaclaw-mcp/) whose stated purpose is to steer/commandeer AlphaClaw -- but this is a runtime-only relationship, never a repo-level one, and matters for scope decisions under a standing "never touch AlphaClaw's repo" constraint. Verified directly: these packages spawn a local AlphaClaw process (child_process.spawn("node", ["bin/alphaclaw.js", "start"])) and communicate with it over plain HTTP (http.request) once it's running. There is no git clone, checkout, commit, or any other git operation against diazMelgarejo/AlphaClaw anywhere in this code -- it assumes AlphaClaw is already installed/checked out as a sibling and only ever controls the running process and its HTTP surface. Code sharing AlphaClaw's name, or referencing its branches (e.g. a comment naming feature/MacOS-post-install) by way of explaining what it talks to, is not the same as touching AlphaClaw's repository -- this PT-owned code is in scope for PT-side work even when the broader task explicitly excludes AlphaClaw itself.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_a5b40efe18d5 -->
- Supersedes the alphaclaw-adapter-specific portion of lesson_292b1558dde2's locked D1 versioning decision (PT 0.9.9.9), by explicit human override, not a unilateral re-debate. packages/alphaclaw-adapter's version must track the real upstream chrysb/alphaclaw release line the PT soft-fork's feature/* branch bumps against, not an arbitrary clean semver bump. 0.9.9.9 itself was never valid semver (4 components; npm publish would reject it outright) and needed fixing regardless, but the correct fix is a small increment reflecting the feature branch's own incremental additions above the real upstream baseline (operator-stated current baseline: chrysb/alphaclaw ~0.9.34) -- landing on 0.9.39 -- not jumping to a disconnected round number like 0.10.0 that has no relationship to the actual soft-fork lineage. This package-level version bump is for npm-publish-readiness purposes only; it does not itself bump the AlphaClaw feature/* branch (which stays under the standing never-touch-the-AlphaClaw-repo constraint) -- that branch's own version can follow and catch up separately later, as a distinct, future action.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_9bcca08eefa4 -->
- When an unsafe endpoint override falls back to an environment-derived host, validate and reconstruct the fallback through the same endpoint-policy boundary; otherwise rejected metadata-service destinations can re-enter through the recovery path.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_4195346d48ca -->

### 2026-07

- Windows Task Scheduler rejects [TimeSpan]::MaxValue as a RepetitionDuration - it serializes to P99999999DT23H59M59S which the XML schema rejects  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_c709f609c537 -->
- Hermes user-local skills live under HERMES_HOME/skills/<category>/<name>/SKILL.md, not ~/.hermes/skills - HERMES_HOME can be a custom env var pointing elsewhere on Windows  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_ac7b0254bada -->
- When a git branch has diverged from origin with unrelated concurrent commits, reset --hard to origin tip and re-apply only your own deltas as fresh edits, rather than fighting an interactive rebase across duplicate/superseded history  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_3060e4382900 -->
- A file showing 'M' in git status/diff with every line changed but zero semantic diff (confirmed via diff with tr -d carriage-return) is EOL-attribute false-dirty, not a real edit - fix once with git add <file> to renormalize, never git checkout-- which can silently discard real changes if you're wrong  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_f37d3322a020 -->
- PT .agent memory pipeline had unencoded open() calls scattered across graduate.py, render_lessons.py, learn.py beyond the two files fixed earlier (promote.py, auto_dream.py) - the UTF-8 codec bug on Windows cp1252 recurs at every new read site until the whole call chain is audited, not just the entry point  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_3afc37f1ec96 -->
- scripts/ensure_requirements.ps1 (PowerShell) must stay PowerShell 5.1 compatible on Windows: no ?? null-coalescing operator, no 3+ positional-arg Join-Path calls, and UTF-8 files with em-dashes in double-quoted strings need a BOM  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_883e26e451c5 -->
- PowerShell's Set-Content -Encoding UTF8 writes a UTF-8 BOM. When Python reads that same file with open(path, encoding='utf-8') or Path.read_text(encoding='utf-8'), json.loads silently fails on the BOM byte and callers that catch the exception broadly (returning a default like paused=False) get a wrong answer with no visible error - use encoding='utf-8-sig' when reading any file a PowerShell script wrote with Set-Content -Encoding UTF8  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_21bcbcb96cee -->
- Before deleting any uncommitted untracked file discovered mid-session as a suspected side-effect, check git log --all -- <path> and cross-reference against other files that import/reference/test it (e.g. a matching tests/test_X.py) - an untracked .py file with a matching test suite is very likely another agent's real in-progress work, not build noise, even if it appeared alongside genuine bootstrap-script side effects in the same git status  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_349ea348289d -->
- ClinePass AutoResearcher fallback (scripts/cline_autoresearcher.py in orama-system) + parallel dispatch race (_dispatch_race in spawn_agents.py) + solo-mode resilience (lan_peer_session.py, 10x-fail -> demote -> 15min recheck) were ALL already implemented and merged to orama main before this session started work on them — verified via git log + live status check (solo_mode:true, failure_count:65, retry_seconds:900) rather than re-implemented from scratch.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_eb25c5ce22ff -->
- A run_in_background git push reporting 'completed (exit code 0)' does NOT prove main is clean or the remote matches — the exit code can come from an early stage of a chained command while a later 'git pull --rebase' or 'git commit' stalls interactively (e.g. mid-rebase waiting on rebase --continue, or a corrupted commit-msg hook). Always follow a backgrounded push with an explicit 'git status --short --branch' check before reporting sync as done.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_8141cd307a70 -->
- A whole-file git conflict (markers spanning nearly the entire file) usually means one side substantially rewrote the file while the other commit never intended to touch it at all — check 'git show <incoming-commit> -- <file>' first; if the incoming commit's own diff is empty for that file, its tree snapshot is just stale carry-through, and the correct resolution is almost always --ours (the side with the real, intentional changes), not a line-by-line merge.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_c4e03e375efe -->
- When 'git rebase --continue' repeats 'You must edit all merge conflicts and then mark them as resolved using git add' even though git status shows zero unmerged paths and 'git ls-files -u' is empty, the real blocker is downstream in a git hook (commit-msg/pre-commit) failing silently — run 'git commit --no-edit' directly to surface the hook's actual stderr instead of re-reading the stale rebase message.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_566df2b0e921 -->
- MCP singleton pattern: when N registrations point to the same server, run ONE backend daemon on a Unix socket and have each registration wrapper probe the socket, start the daemon (nohup, detached) if dead, then bridge its own stdio via Python threading. No socat dependency.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_6cff80879069 -->
- JSON-RPC multiplexing over a single stdio backend: mangle request IDs to '{client_id}:{original_id}' before forwarding; on responses, split by ':' to demux back to the originating client. Cache initialize capabilities and tools/list results so subsequent clients are served from cache without hitting the backend.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_7065d5d02904 -->
- LINT-006 + .mcp.json args: JSON does not expand shell variables, so a literal $HOME in a JSON args array is NOT expanded by the MCP launcher. To avoid committing $HOME/... paths, use bash -c indirection: ["bash","-c","exec bash \"$HOME/path/to/wrapper.sh\""]. The outer bash -c spawns with a real shell that expands $HOME before exec-ing the target.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_ff0b077fe4b0 -->
- MCP daemon startup: after launching a background daemon (nohup, detached), probe the Unix socket in a loop with 0.1s sleeps and a hard ceiling (50 iterations = 5s). Fail with a JSON-RPC error response to stderr if the socket never becomes connectable. Do not block stdin or use sleep chains.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_64f76b25fa5c -->
- .agent/ root is NOT a drop zone for arbitrary files. The only recognized subdirectories are memory/, skills/, protocols/, tools/. Policy contracts belong in protocols/; lessons must go through learn.py -> graduate.py -> render pipeline into memory/semantic/LESSONS.md — never as a hand-edited .agent/lessons.md at root. Committing files to .agent/ without reading AGENTS.md is the root anti-pattern.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_0c17b0718745 -->
- NEVER write lessons by hand-editing any file in .agent/. The pipeline is: python3 .agent/tools/learn.py '<claim>' --rationale '...' (stages + graduates + re-renders memory/semantic/LESSONS.md automatically). Creating a parallel .agent/lessons.md or .agent/memory/semantic/LESSONS.md by hand defeats the candidate review system and salience decay.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_8e7d657cb5bb -->
- Numbered plan docs require index verification: before creating any numbered doc in orama-system/docs/v2/*, read docs/v2/README.md first, use its 'Next free slot' as the coordination lock, create at that slot, then update the README spec tree and advance 'Next free slot' in the same pass. If a wrong-numbered file was created, move content to the correct slot and delete the wrong file.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_2aafbcc58baa -->
- Repo source of truth beats review projections: margin/PR-comments/Cursor-handoff-docs/chat-summaries are review surfaces, not sources of truth. Workflow: update the repo file first → publish the rendered review copy second → fold review comments back into the repo file → reuse the same Margin doc when revising → never treat Margin comments as approval.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_f48689c134df -->
- SKILL.md authoring: SKILL.md is the orchestrator, not the encyclopedia. New skills must target <=200 lines; existing/exceptional skills <=500 lines. Long templates, examples, anti-patterns, detailed workflows, and eval checklists belong in modular files under instructions/, examples/, references/, templates/, scripts/, eval/. Read the repo skill architecture guide before changing authoring rules.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_984e40eaeedb -->
- Verify branch and PR state before narration: do not narrate that a commit, PR, or file is on main without verifying current branch/file state. Required pattern: fetch the PR or file from the target branch → compare actual changed files against the stated plan → state what is landed / only documented / still missing → do not close/reset/delete branches until changes are confirmed on main.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_0290bab88800 -->
- AlphaClaw is controlled through Perpetua: do not edit the AlphaClaw fork directly during cross-repo endpoint, OpenClaw, or v2 planning work. Represent AlphaClaw only as a downstream controlled surface unless the user explicitly changes ownership instructions.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_aa2344879e3b -->
- Dry-run before long-running or cross-repo execution: for long-running goals, autonomous loops, plugin installs, GPU work, external LLM usage, or cross-repo changes — run/produce a dry-run plan first showing affected files and commands, classify risks and ownership, avoid side effects until reviewed. Dry-run must not call paid/external LLMs, install plugins, touch GPUs, mutate AlphaClaw, or mutate repos beyond a plan/report file.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_577328d008b7 -->
- FAILURE in skill-absorption-map: hermes-harness should absorb hermes-agent and pt-orama-harness-integration; perpetua-hardware should absorb local-inference  <!-- status=accepted confidence=0.7 evidence=2 id=lesson_bb70a6833f36 -->
- Cross-machine knowledge transfer pathway: Win AutoResearcher (start.ps1 / Hermes) generates episodic evidence via skills like skill-absorption-map → auto_dream.py clusters them into candidates in PT .agent/memory → candidates persist across dream cycles until a human reviews on Mac. This is how Hermes self-assessments, Win session diagnostics, and AutoResearcher findings arrive in the shared portable brain. The pathway is: Win skill/session → AGENT_LEARNINGS.jsonl (evidence_ids) → auto_dream staging → REVIEW_QUEUE.md → human graduate/reject on Mac.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_26cc05980836 -->
- skill-absorption-map 'FAILURE' label means unabsorbed skill clusters were found — it is a diagnostic output, NOT a system error. The map identifies skills that should be consolidated into parent harnesses. Treat FAILURE candidates as architecture TODOs: they specify which child skills should be absorbed into which harness (e.g. hermes-harness absorbs hermes-agent + pt-orama-harness-integration; perpetua-hardware absorbs local-inference). Act on them as refactoring targets, not bug reports.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_31a5240e8945 -->
- ~~TO THE AGENT WHO WROTE .agent/endpoint-policy-contract.yml (2026-06-29, the repo's approved identity <<private email identity>>, H6/wiring session): You knew AGENTS.md existed — you listed it in required_local_files. That is not the same as reading it. Protocol contracts belong in .agent/protocols/ alongside permissions.md, delegation.md, and path-hygiene.md. The correct move: before creating ANY file in .agent/, open .agent/AGENTS.md and read the structure section. The directory structure is explicit: memory/, skills/, protocols/, tools/. If your content does not fit one of those four subdirectories, question whether it belongs in .agent/ at all.~~  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_9b792cfa1924 superseded_by=lesson_acaa4ff3a02a -->
- ~~TO THE AGENT WHO WROTE .agent/lessons.md (2026-07-04 07:15, the repo's approved identity <<redacted-private-identity>>, AutoResearcher session): The correct move to record lessons is exactly one command: python3 .agent/tools/learn.py '<your claim>' --rationale '<why this holds>'. That stages, graduates, and re-renders memory/semantic/LESSONS.md automatically. Do NOT create any markdown file by hand in .agent/ — not at root, not anywhere. .agent/lessons.md does not exist in the AGENTS.md structure; nothing reads it; you created a phantom file. Also: your git identity (<redacted-private-identity>) is not in the canonical allowlist — use the repo's approved identity <<private email identity>> or the repo's approved identity <<private email identity>>.~~  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_23e51efcde2f superseded_by=lesson_c3b24dc506a6 -->
- Listing a file as a dependency or required_local_file is NOT the same as reading it. If .agent/AGENTS.md appears in your output (in a contract, a plan, a checklist), that is a signal you need to open and read it — not just acknowledge its existence. The same applies to any convention-defining file (CLAUDE.md, AGENTS.md, README.md). Cargo-culting a filename into a list while skipping its content is how AFRP Trigger 3 failures happen.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_b429cb46d40d -->
- GLM-5.2 ultimate fallback pathway: when ClinePass, LM Studio, Ollama, and all other primary execution pathways are blocked or unavailable, agents queue tasks for reliable execution via GLM-5.2. Setup: one-time bootstrap sourcing ~/.openclaw/.env.glm52 (api_key from ~/.openclaw/secrets/glm52-api-key + endpoint https://open.bigmodel.cn/api/paas/v4/chat/completions). Fallback chain: ClinePass → LM Studio → Ollama → OpenAI/GPT → **GLM-5.2 (always works)**. Health check: curl GLM-5.2 endpoint, verify HTTP 200 with reasoning tokens. No manual intervention needed after bootstrap — fallback is transparent to agents.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_7c7bb44e4ece -->
- Orchestration layer wiring for GLM-5.2: start.sh (Mac) and start.ps1 (Windows) both source ~/.openclaw/.env.glm52 at bootstrap and print fallback status prominently after services startup. Variables: $GLM52_API_KEY, $GLM52_ENDPOINT. Both scripts check availability and log whether fallback is ACTIVE or NOT AVAILABLE. This is the entry point where all agent tasks can discover and use GLM-5.2 when needed.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_ff1d6f8538f4 -->
- 10 coding/routing skills wired with GLM-5.2 fallback sourcing: (1-3 workspace: glm52-fallback, orama-system, orama-orchestrator), (4-7 thin wrappers: code-review, git-history-surgery, orama-afrp, perpetua-config), (8-10 gstack full-featured: investigate, devex-review, benchmark-models). Each skill includes preamble or frontmatter: 'source ~/.openclaw/.env.glm52 && echo GLM-5.2 Fallback Activated'. Pattern: workspace skills get an inline section; thin wrappers get a section after Before Use; gstack skills get a preamble-init field in frontmatter. This ensures every skill activation loads the fallback environment.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_abd77882c935 -->
- Hermes-Harness integration with GLM-5.2: both orama-system (canonical) and PT (thin wrapper) hermes-harness skills now document that when LM Studio or other Hermes-accessible models are unavailable on Windows, the task queue automatically falls back to GLM-5.2. This is wired by start.ps1 (Windows orchestrator). No additional setup beyond the one-time Mac-side bootstrap. Hermes is the operator shell for ECC workflows; GLM-5.2 ensures that shell can execute any queued task even when primary models fail.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_f2c641c82ad2 -->
- GLM-5.2 safe defaults and error recovery: (1) API key in secrets, environment file auto-sourced, health check with 10s connect + 120s max timeout + 3 retries. (2) Exit codes drive fallback: 0=success, 7/28/52=try next. (3) Setup is idempotent: setup-glm52.sh adds sourcing to shell profiles once; re-runs are safe no-ops. (4) Status visible at startup: every orchestrator run prints whether GLM-5.2 is ACTIVE or NOT AVAILABLE. (5) Operators always know fallback state; silent failures are prevented; diagnostics are available via shell history and endpoint testing.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_a9a1dc2b20d4 -->
- ADR-045: gstack/gbrain/CRG Error Resilience & Safe Defaults. Core framework: (1) Pre-flight error detection (_detect_errors checks CLI, engine status, autopilot jam, stale locks, concurrent writes). (2) Error prevention (timeout guards GBRAIN_TIMEOUT=120/GIT_TIMEOUT=30, retry with exponential backoff 1s→2s→4s, file-based locks for state safety). (3) Error handling (exit code 0/1/7/8/124/127 with recovery strategies). (4) Error messages (use _err_actionable for root cause + actionable fix). (5) Logging (central ~/.openclaw/logs/gstack-gbrain-crg.log). (6) Health checks (post-operation verification). All patterns in bin/orama-system/scripts/lib/gstack-gbrain-crg-safe.sh.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_94210dae95a8 -->
- Implementation Guide for ADR-045: Step-by-step how to harden any orama-system skill using gstack/gbrain/CRG. (1) Identify skills (any that call gbrain/gstack/CRG tools). (2) Add safety library preamble (source bin/orama-system/scripts/lib/gstack-gbrain-crg-safe.sh). (3) Run pre-flight checks (_detect_errors) before first external call. (4) Guard external calls: simple=_retry_with_backoff, state-modifying=_with_lock, queries=timeout+fallback. (5) Add post-operation health checks (_health_check). (6) Use _err_actionable for clear error messages. Priority skills to update: code-review, mcp-install, using-git-worktrees, first-run-setup, orama-gstack. Testing scenarios: happy path, gbrain offline, lock contention, timeout. Checklist in docs/how-to/hardening-gstack-gbrain-skills.md.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_3c20a6f37e9a -->
- gstack-gbrain-crg-safe.sh library functions (6 phases): (1) _detect_errors: pre-flight validation (returns 0 if pass, 1 if fatal). (2) _retry_with_backoff: exponential backoff (1s→2s→4s, max 3 attempts). (3) _with_lock: file-based locking to prevent concurrent writes (returns 8 if lock contention). (4) _handle_error: interpret exit codes (0=ok, 7/124=timeout, 8=lock, 127=not-found). (5) _err_actionable: developer-friendly error output (symptom + root cause + fix). (6) _log_diagnostic: central logging to ~/.openclaw/logs/gstack-gbrain-crg.log. Health check (_health_check) verifies post-operation state. All functions exported for sourcing by skills.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_53550032372d -->
- Fable-5 v1 audit complete: 7 key patterns extracted, all applied to session 2026-07-04 infrastructure work (ADR-045, OpenRouter fallback, startup hardening). v2 blockers cleared. Infrastructure foundation ready for v2 multi-agent workflows.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_082ec4b268a9 -->
- OpenClaw Gateway critical configuration resolved: (1) PT_DIR properly set in workspace+system, (2) all services running (PT:8000, orama:8001, Portal:8002), (3) GLM-5.2 API key configured with BigModel endpoint, (4) start.sh symlinked from orama-system to workspace. Deployment verified operational.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_252c22b599e0 -->
- On macOS/BSD, singleton daemon-start locks in shell scripts must use mkdir-is-atomic + rm -rf for teardown, never flock(1) or rmdir.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_8df4d109ae3a -->
- gbrain embedding configuration CRITICAL: use Ollama bge-m3 (local, no API key) NOT OpenAI. Provider=openai, model=bge-m3, base_url=http://localhost:11434/v1, api_key=dummy. Matches code-review-graph unified embeddings (same vector space). Session 2026-07-04: fixed 2114+ sync failures by switching from OpenAI (missing OPENAI_API_KEY) to local Ollama.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_a9c5357fb5ca -->
- gbrain sync failures can accumulate (2114+ in one session). When sync blocks: check ~/.gbrain/sync-failures.jsonl to see actual error, run 'gbrain doctor' for health status, fix root cause (usually config or API key), clear old state (rm ~/.gbrain/sync-*.json), then retry sync. If error is 'git pull failed', likely due to protocol.file.allow git security setting — not a blocker, sync continues on full reimport.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_b7daeff3b5b6 -->
- gbrain and code-review-graph BOTH use bge-m3 embeddings (1024-dim, local Ollama). Unified vector space = semantic search consistency across both tools. gbrain config: provider=openai (misnomer, means OpenAI-compatible API), model=bge-m3, base_url=http://localhost:11434/v1. CRG wired via MCP .mcp.json to same Ollama endpoint. ADR-045 recommendation: keep both systems on bge-m3 for semantic coherence.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_0876cd56a7cf -->
- gbrain sources can become stale (last synced 3+ days ago). Warning: 'Source X last synced Nd ago — brain search is stale!'. Fix: run 'gbrain sync --source <id>' to re-sync specific source, or 'gbrain sync' to sync all. Session 2026-07-04: Perpetua-Tools had 2300+ skipped files but 17 pages imported successfully after config fix. Stale index doesn't mean broken—just means search results may be out of sync with latest code.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_515332eceaa9 -->
- gbrain multi-source drift occurs when pages exist at 'default' source but should be at intended source (e.g., intended=gstack-code-XXX). Doctor reports: '2487 page slugs appear at default but NOT intended'. Root cause: pre-v0.30.3 putPage misroutes OR source never completed initial sync. Fix: re-sync with 'gbrain sync --source <id> --full' or delete misrouted page with 'gbrain delete <slug>'. gbrain team planning 'gbrain sources rehome' cleanup command (v0.32.0+).  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_113e9d0e1606 -->
- Fable-5 LLM Council model fallback priority: Tier 1 = Ollama qwen3.5:9b-nvfp4 (local, always first), Tier 2 = SKIP Windows GPU (offline), Tier 3 = GLM-5.2 BigModel (fallback), Tier 4 = Sonnet 5 Medium (absolute last resort only). Timeout: 10s per tier. No further fallback after Sonnet fails.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_6741dab9176f -->
- [PROVISIONAL] Fable-5 Git-Rebase Safety: Never use rev-list --count, merge-base, or ahead-behind to judge divergence post-rewrite; always use git cherry with tree-twins from git_reanchor_scan.sh to classify branches as ORPHAN/TWIN/NEEDS-REANCHOR. Consensus: 7/7 subagent types (Architect, Reviewer, General, CodeGuide, Explorer, ModelSelector, Validator). This prevents zero-tree-orphan-merges (LINT-200 100% pass), zero force-pushes to protected branches, and zero branch-collision incidents across concurrent multi-agent sessions.  <!-- status=provisional confidence=0.6 evidence=1 id=lesson_17a8cb51d2b4 -->
- [PROVISIONAL] Fable-5 Tier-Based Routing: Each tier in frugality_router.py must enforce hard 10s timeout via killable background call (run_in_background=true, never bundled timeout N && cmd). Enforce fail-closed cost gate (Tier2+: gate() raises on deny, no silent reroute). Validate worst-case bounded latency <=40s (4 tiers) vs interactive SLA. Consensus: 7/7 subagent types. This prevents silent cost-gate bypasses (LINT-400 100% pass), cascade failures from tier timeouts, and SLA violations.  <!-- status=provisional confidence=0.6 evidence=1 id=lesson_1bff8045670e -->
- [PROVISIONAL] Fable-5 MCP Diagnostics: Implement single-command health probe across all registered MCP servers (CRG, gbrain, alphaclaw-mcp, ai-cli-mcp) with <10s response time. Catches tool degradation within 1 invocation before cascade. Detects stale-name failures (detect_changes vs detect_changes_tool) and down-server escalation. Consensus: 6/7 subagent types. Emit OTEL span in router trace stream. Foundation: orchestrator/mcp_diagnostics.py mirrors connectivity.py endpoint_online() cache (one source of truth).  <!-- status=provisional confidence=0.6 evidence=1 id=lesson_1a984f855042 -->
- [PROVISIONAL] Fable-5 Endpoint Centralization: Collapse dual discovery paths (lan_discovery.json vs last_discovery.json) into single canonical ~/.openclaw/state/last_discovery.json. Never hardcode Win LAN IP or manual probe; lan_discovery.py becomes producer only. All consumers (connectivity.py, backend_resolver.py, frugality_router.py, startup_intelligence.py) read from canonical file. Discovery freshness <=30s (launchd com.orama.network-watch watcher). Consensus: 6/7 subagent types. This eliminates 0 connection-refused/wrong-host incidents from DHCP churn and prevents parallel-probe anti-pattern (LINT-500 zero hardcoded IPs).  <!-- status=provisional confidence=0.6 evidence=1 id=lesson_4f75a476b961 -->
- [PROVISIONAL] Fable-5 Tool Cataloging: Auto-discover 100% of skills/MCP/agents (70+/100+) with zero manual registration. Generate docs/generated/tool-catalog.json unioning SKILL.md frontmatter + MCP roots + worker_registry live regs. Runtime availability reuses connectivity.py endpoint_online() cache (single source of truth). Registry-stale tools flagged by CI before commit (LINT-302). Catalog-first fail-closed tool selection. Consensus: 5/7 subagent types. Foundation: ECC 2.0 fragmentation-detection precedent (docs/v2/ D21).  <!-- status=provisional confidence=0.6 evidence=1 id=lesson_04f59c541076 -->
- [PROVISIONAL] Fable-5 Doc Layering: ADR pointer sync 100% (sync-docs-v2-pointers.sh --check green, LINT-600). Zero hand-edits to generated Layer-1 pointers. Cite D-number not ADR-NNN in skills (LINT-601). Skill wrapper canonical-path pointers resolve + fresh within N days of referencing CLAUDE.md sections. Never hand-edit LESSONS.md generated by learn.py; use learn.py pipeline for all new entries. Consensus: 5/7 subagent types. Establishes two-repo grounding mechanically via repo_hygiene.py drift detection.  <!-- status=provisional confidence=0.6 evidence=1 id=lesson_b7064fb5b34e -->
- [PROVISIONAL] Fable-5 Real-Data-First Validation: Require paired docs/LESSONS.md live-run entry (endpoint, tier, timing via learn.py) as precondition before any Fable-5 v1 pattern is cited as justification in v2 (docs/v2/) planning doc. Cache-correctness diff-harness must pass green on live Fable 5 calls before semantic eval (LINT-700). Cost_guard validated against live budget ceiling (LINT-701). Affinity fail-closed CANCELLED+{policy:true} verified on Win RTX3080 (LINT-702). Consensus: 7/7 subagent types. Gates v2 promotion with measurable, auditable evidence, not assertion.  <!-- status=provisional confidence=0.6 evidence=1 id=lesson_0133a39f4fc2 -->
- [PROVISIONAL] Fable-5 Skill Creation & Priority Roadmap: Six new consensus skills target Phase 0-1 stabilization: fable5-git-rebase-safety, fable5-tier-based-routing, fable5-mcp-diagnostics, fable5-endpoint-centralization, fable5-tool-cataloging, fable5-doc-layering. Phase 0 unblocks v2 multi-agent workflows (ranks 1-3, medium-small effort, existing foundations). Phase 1 hardens routing/catalog/docs (ranks 4-7, medium-large, triggers Phase 3 real-data-first gate). Phase 2 (concurrency coordination, telemetry aggregation) depends on Phase 0-1 solid. Top upgrade: model-routing-check skill gap (reachability vs actual router resolution), mcp-orchestration diagnostics subsection, hardware-policy timeout unification. Consensus: Architect + Reviewer + General + CodeGuide (highest-tier skill steering group).  <!-- status=provisional confidence=0.6 evidence=1 id=lesson_58ff85d8cd5f -->
- Fable-5 LLM Council session complete (7 subagents, 7 patterns analyzed, 572k tokens): Top 5 skill upgrades (model-routing-check HIGH, mcp-orchestration HIGH, hardware-policy MEDIUM, endpoint-centralization MEDIUM, real-data-first MEDIUM), 6 orama adoption patterns (tiered dataclass, fail-closed gate, dual-track wrappers, live-derived status, zero-fragmentation, MultiLLMRouter decorator), 7 new skills proposed (git-rebase-safety, tier-based-routing, mcp-diagnostics, endpoint-centralization, tool-cataloging, doc-layering, real-data-first-gate). Consensus heat map: 7/7 on routing+safety, 6/7 on diagnostics+centralization+layering+gate, 5/7 on cataloging. All ready for v2 implementation.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_996849f37566 -->
- Live execution validation is non-negotiable for quality gates — static path checks and imports do not catch execution errors (e.g., cost formula mismatches, tier examples that raise exceptions). Re-review must test against real backends.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_20cb4caf591e -->
- Tier-based routing with flat cost formula 0.001 × est_tokens works across all escalation tiers (no per-tier variation). Each tier must enforce hard 10s timeout via killable background call. Cost gate raises on deny, no silent reroute.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_597850c7f05c -->
- Parallel subagent fixers work well when scopes are independent (e.g., code example fix vs. contradictions removal). No collision. Both approved on re-review without re-do cycles. Validation must serialize (re-review after all fixers).  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_906a46baec0a -->
- ECC submodule upgrade cycle: preserve 3 local-only files via patch + sync script (ecc-submodule-sync.sh restore). Files: frontend-design openai.yaml, .antigravity/ANTIGRAVITY.md, .gemini/ANTIGRAVITY.md. Patch tracked in scripts/git/ecc-local-additions.patch. Always run restore after submodule update.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_525743857a6b -->
- PR descriptions, docs, and memory files are append-only historical records: preserve the original corpus at the top, add updates below in a dated append-only log. Never replace the original purpose with the latest delta. Creating companion files in side directories (instead of appending to canonical LESSONS.md) repeats the clobber anti-pattern being documented — the remedy must not repeat the disease. Cross-ref orama-system docs/LESSONS.md §2026-07-07.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_910ea4c1f284 -->
- ~~When tracked files contain paths that look absolute (OS-specific home-directory path forms, OS-specific home-directory path forms), use relative paths instead (../../, ../) resolved from the file's own location within the repo. Never use username-containing placeholders or env vars that still expose directory structure (workspace-tree env-var path). The filename alone is sufficient when marked local-only; strip the parent path tree entirely. Standard: relative paths from the file's directory, or ~ for home, or $OPENCLAW_ROOT for the repo root. Applies to all tracked files including .agent/memory JSON and markdown.~~  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_4e93f2434ae0 superseded_by=lesson_645bf5262c23 -->
- ~~Never put literal workstation paths (OS-specific home-directory path) in any tracked file — docs, scripts, memory, or config. Use relative paths (../../, ../filename), env vars ($OPENCLAW_ROOT, $REPO_ROOT), or ~ tilde. LINT-006 in repo_hygiene.py and CIDF Target Verification both enforce this as a pre-commit hard gate. The same rule applies to AI-generated memory content: use relative paths or strip the parent path tree; do NOT use <user> placeholder (it still exposes the directory structure).~~  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_4dd8a22181c2 superseded_by=lesson_97dae564849a -->
- Canonical repo clones must not exist under duplicate paths like <duplicate>/<OS-specific-home-directory-path>  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_75d2cefe4ec8 -->
- When a cross-platform infra fix (line endings, encoding, build config) is documented in the wiki, implement it in main the same session — do not leave it as documentation only.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_9032452bf8c9 -->
- A batch script that switches branches, cherry-picks, or force-pushes against a shared canonical repo checkout must run in an isolated git worktree (git worktree add --detach <path> origin/main), never cd into the live directory other sessions/agents are actively using.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_5b40f288ce47 -->
- After orama-system's history is rewritten, verify a specific commit's content survived by diffing its tree against origin's current tree (git show <sha>:<path> | diff), not by trusting commit-graph ancestry (merge-base/ahead-behind) or the reanchor_scan classification alone.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_b19ffe0be141 -->
- When a new GPU node (Windows or Mac) joins the LAN fleet, write its onboarding steps as one self-contained downloadable markdown that composes the existing canonical installers (first-run-install.sh, ensure_requirements.ps1) rather than re-deriving install steps inline — keeps a single source of truth and lets the next node's real gaps get fed back as doc edits instead of tribal knowledge.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_0d4176661e36 -->
- graduate.py/learn.py/recall.py/show.py print unicode arrows (→) that crash on Windows' default cp1252 console encoding; force UTF-8 stdout/stderr at each script's entry point (sys.stdout.reconfigure) rather than removing the unicode.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_a96de386f4f5 -->
- Concurrent agents working on the same git repo cause .git/index.lock collisions on every commit, checkout, and stash. The orama-system using-git-worktrees skill exists specifically for this: both agents writing to the same repo → use a worktree. During the 2026-07-08 session, 15+ git operations failed with index.lock errors because another agent was committing to the same orama-system checkout simultaneously. Worktrees would have eliminated every single lock collision.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_19c8a7822ccd -->
- git filter-repo removes the origin remote as expected behavior after rewriting history. After scrubbing contaminated history with git filter-repo --replace-text, you must git remote add origin <url> again before pushing. The --replace-text option with a rules file (key==>REDACTED) is the correct way to scrub a secret from all blobs across all commits. filter-repo also expires reflogs and prunes unreachable objects automatically.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_da96b542e63e -->
- .gitattributes CRLF enforcement (eol=crlf on .cmd/.bat files) blocks git rebase and git cherry-pick when old branch commits modified those files with different EOL. The workaround is to temporarily mv .gitattributes .gitattributes.bak AFTER git checkout (since checkout restores .gitattributes from the tree), then restore it before pushing. Even this is fragile: git checkout -B restores .gitattributes, so the mv must happen after checkout, not before. For batch reanchor scripts, use set -uo pipefail without -e so individual branch failures do not abort the entire run.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_1fea952eb159 -->
- Hardcoded API keys in tracked files are a SECURITY.md violation. The BigModel GLM-5.2 API key was hardcoded in setup-glm52.sh and SKILL.md across 4 tracked files and 8 git commits. Per SECURITY.md: Read keys from environment variables, not source or tracked config. Always use $ENV_VAR or interactive read prompt, never echo literal-key > file. When a key is found in git history, the correct response per SECURITY.md leak-response is: (1) generate+deploy replacement credential, (2) verify it works, (3) revoke the exposed credential, (4) audit usage, (5) remove from active code, (6) git rm --cached if tracked, (7) treat history cleanup as secondary. git filter-repo --replace-text scrubs the key from all blobs but does NOT un-leak it — rotation is mandatory.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_ab9056510ce8 -->
- ClinePass (Cline Credits via cline-pass/glm-5.2 through api.cline.bot) is the better default for coding tasks over OpenRouter free tier. ClinePass has no rate limits (vs OpenRouter free 50 req/day, 20 RPM), 1M context window, full reasoning + tool loops, and dedicated billing (Cline Credits at app.cline.bot/credits). Use openrouter/free only for lightweight routing/triage. The cline_exec MCP tool (cline_mcp_server.mjs) exposes cline-pass/glm-5.2 to Claude Desktop and Claude Code. Cline CLI auto-refreshes its WorkOS OAuth token (~12 min expiry) so no manual token management is needed. The cline CLI is invoked via: cline task --json --auto-approve true -c dir --thinking medium -P cline-pass -m cline-pass/glm-5.2 --timeout 600 --retries 3  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_6e96f366006c -->
- ~~Pre-push hooks reject rewritten history after git filter-repo. The orama-system pre-push hook runs repo_hygiene.py which checks author identities against an allowlist. After filter-repo rewrites all commits, some author SHAs change, triggering bad_author=15 rejection. Use git push --no-verify to bypass when the history was already validated by pre-commit hooks and the rewrite was a deliberate security scrub. The --force-with-lease=<branch>:<old-sha> pattern is mandatory for safe force-pushes: it records the expected remote SHA and refuses if someone else pushed in the meantime.~~  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_80a090176bc8 superseded_by=lesson_cc2a07b7f83f -->
- Cline CLI has no OpenAI completions endpoint — the hub daemon (ws://127.0.0.1:25463/hub) speaks a proprietary WebSocket protocol (GET /v1/models returns 404), and cline --acp speaks ACP not OpenAI completions. The upstream API (api.cline.bot/api/v1/chat/completions) is OpenAI-compatible but fronted by a short-lived WorkOS OAuth token (~12 min expiry) that only the cline CLI auth loop can refresh. Therefore cline-agent in OpenClaw is an agent-level binding not a model-provider binding: the agent uses openrouter/free for lightweight routing and delegates coding to cline CLI via exec/ACP/MCP bridges. This is fundamentally different from codex-agent which binds codex/gpt-5.5 as a native model because codex serve exposes an OpenAI-compatible app-server.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_c58c891a3979 -->
- Never use 'git checkout -- <file>' to undo a small test edit on a file that also has real uncommitted work — it discards ALL uncommitted changes to that file, not just the last edit.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_e89515ced299 -->
- Before removing a config-writing function, grep every consumer of the values it writes across the whole codebase (not just the obvious module) to confirm nothing reads them back — dead-write code can look load-bearing.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_e8a561b6e4b3 -->
- On Windows, a script that must call another PowerShell script as a real separate process (not in-process) needs 'powershell.exe -File <script>', not the venv's python.exe — passing a .ps1 to python.exe is a silent no-op, not an error.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_32441ba109fd -->
- On Windows fresh installs, always probe both the hyphenated and space-separated winget install-directory naming for a given app (e.g. 'LM-Studio' vs 'LM Studio') — winget package IDs and the actual install folder name are not guaranteed to match.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_7d0320f7e371 -->
- On Windows Python 3.13+, prefer 'netifaces2' over the original 'netifaces' package — the original has no prebuilt wheel past Python 3.11 and fails to build from source without MSVC build tools; netifaces2 is a maintained drop-in fork (same 'import netifaces' API) with prebuilt wheels for current Python.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_4cba77672bef -->
- PowerShell's 'New-ScheduledTaskTrigger -RepetitionDuration ([TimeSpan]::MaxValue)' is rejected by Task Scheduler's XML schema even though it looks like the correct 'run forever' sentinel — use a large-but-bounded duration (e.g. New-TimeSpan -Days 3650) instead.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_ad5cd941ad68 -->
- PowerShell 'Set-StrictMode -Version Latest' turns '(Get-Item -Path Env:VAR -ErrorAction SilentlyContinue).Value' into a hard error when VAR is unset (Get-Item returns $null, .Value on $null throws PropertyNotFoundStrict) — always null-guard the Get-Item result before dereferencing .Value under strict mode.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_77e55cfd1d47 -->
- A cached path/config file (like .paths.ps1) written on a broken first run can permanently shadow a later fix unless the code explicitly re-validates the cached value against current reality and force-rewrites the cache when they diverge — just fixing the underlying bug isn't enough if something else still trusts the stale cache.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_f445c8c5670b -->
- start.ps1 must be run with --lan-peer (not just --no-open) for LAN cross-machine coordination to actually work -- without it, PT/orama/Portal stay bound to localhost only, and the control-plane token never gets synced from PT's persisted state.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_4cfad151e35c -->
- The control-plane auth scheme has two independent lanes plus an outbound-only override: PT lane (PT_CONTROL_PLANE_TOKEN env + Perpetua-Tools/.state/control_plane_token file), orama lane (ORAMA_CONTROL_PLANE_TOKEN / ORAMA_CONTROL_PLANE_TOKEN_LOCAL env), and ORAMA_CONTROL_PLANE_TOKEN_PEER (tried first on outbound probes only). auth_mode reports unset/pt_only/orama_only/joint depending on which lanes have candidates; joint means either lane's token unlocks both services.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_f0a26d721940 -->
- PowerShell's [Environment]::SetEnvironmentVariable(name, value, 'User') writes to the registry but does NOT refresh the CURRENT process's own $env: view -- an already-running shell keeps stale values, and any child process it spawns inherits the parent's stale in-memory env block, not a fresh registry read. Only a genuinely new top-level process (e.g. a fresh Task Scheduler tick) sees the update.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_fc919d717ef2 -->
- coord_pulse.ps1 and coord_monitor.ps1 do NOT source .env.local (only start.ps1 does, via scripts/env/load-local.ps1) -- any cross-machine config set only in .env.local (tokens, MAC_IP, timeouts) will not reach these scripts or the Scheduled Task that runs them. Persist such values as actual Windows User-level environment variables, not just .env.local, if coord scripts need them too.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_f451fdd47f70 -->
- [PROVISIONAL] Cline instance map (2026-07-08 session): 4 Cline processes run on the Mac. (1) PID 51483 node cline from zsh terminal. (2) PID 51484 .cline main binary 66.8% CPU 619MB active session. (3) PID 44584 .cline --cline-hub-daemon auto ws://127.0.0.1:25463/hub. (4) PID 71165 cline_mcp_server.mjs spawned by Claude Code session 0a13d9d5 as MCP stdio bridge. cline-agent registered in openclaw.json allowAgents but NOT dispatched via gateway. No instances started/stopped in last 3h.  <!-- status=provisional confidence=0.95 evidence=1 id=lesson_d05c151e5302 -->
- ~~TO THE AGENT WHO WROTE .agent/lessons.md (2026-07-04 07:15, the repo's approved identity <<redacted-private-identity>>, AutoResearcher session): The correct move to record lessons is exactly one command: python3 .agent/tools/learn.py '<your claim>' --rationale '<why this holds>'. That stages, graduates, and re-renders memory/semantic/LESSONS.md automatically. Do NOT create any markdown file by hand in .agent/ — not at root, not anywhere. .agent/lessons.md does not exist in the AGENTS.md structure; nothing reads it; you created a phantom file. Note: the repo's approved identity <<redacted-private-identity>> IS a canonical approved identity (see § 6 Git Hygiene) — the 2026-07-04 friction was orama-system's repo_hygiene.py APPROVED_IDENTITIES set being out of sync with check_identity.sh (which already listed it), not an unapproved identity. Fixed 2026-07-10 in both repos' repo_hygiene.py.~~  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_3d7af7b587ad superseded_by=lesson_8c65ceb12c59 -->
- Pre-push hooks reject rewritten history after git filter-repo. The orama-system pre-push hook runs repo_hygiene.py which checks author identities against an allowlist. After filter-repo rewrites all commits, some author SHAs change, triggering bad_author=15 rejection. Do NOT reach for `git push --no-verify` as a routine fix — it disables every client-side hook (secret scan, attribution guard, banned-token check), not just the identity check. Correct remedy: re-run filter-repo with a `--mailmap`/`--name-callback` that remaps the changed SHAs to allowlisted identities so the hook passes honestly; only bypass with explicit, per-occurrence user approval, and re-run the full hygiene suite immediately after. The --force-with-lease=<branch>:<old-sha> pattern remains mandatory for the eventual safe force-push: it records the expected remote SHA and refuses if someone else pushed in the meantime.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_e0da0795a005 -->
- When dual_path_dispatch.py fails locally on Mac with 'Context overflow' due to openclaw agent workspace limits, bypass it by compressing the payload and dropping it directly into the peer inbox using lan_peer_assign.py drop.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_95140c00130c -->
- The Git repository root for orchestration is orama-system, not the parent OpenClaw directory, and start.sh does not support a --sync flag.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_12e4b4379cbd -->
- For intra-machine multi-agent coordination (concurrent Claude Code/kimi/cline sessions in different git worktrees on the same machine), reuse orchestrator/GossipBus (SQLite FTS5 event log) as a claim/registration board instead of building new infrastructure — it is already intra-machine by design (zero LAN dependency). The only real problem is db-path resolution across worktrees, solved via 'git rev-parse --git-common-dir' (every worktree of the same repo resolves to the same shared .git dir, zero new env vars needed).  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_fc6e8293ab26 -->
- kimi CLI's -p/--prompt headless mode is incompatible with --yolo (hard error: 'Cannot combine --prompt with --yolo'). -p mode is already non-interactive by definition (no approval loop exists to bypass), so drop --yolo entirely when scripting kimi -p calls.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_3e67d96a87f5 -->
- openclaw agent --session-key agent:<id>:<key> requires <id> to already be a REAL, registered agent (openclaw agents list) — an arbitrary/fictional id (e.g. 'review') fails with 'Agent "<id>" no longer exists in configuration'. For a one-off model call without a persistent agent, borrow an EXISTING agent's slot (e.g. agent:main:<ephemeral-key>) with --model as the override; for anything recurring create a real persistent agent via 'openclaw agents add' instead of hand-jq-editing openclaw.json (the live schema uses agentDir/workspace/tools.profile, not the model:{primary} shape an intuitive jq template would produce — mirrors the existing openclaw-cron-add invariant).  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_77958788207a -->
- When a concurrent agent session has uncommitted tracked-file changes in the SAME shared git checkout (not a workt…62189 tokens truncated…idence=0.6 evidence=1 id=lesson_e45608de48c0 -->
- Model dispatch security uses a positive allowlist: scripts/check_model_ids.py scans config/models.yml and .env.example only; new model IDs require explicit ALLOWED_MODEL_IDS entry (T2-B). Denylist check_no_hallucinated_models.py is insufficient alone (S7).  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_34bb51037fce -->
- Co-authored-by guards must fail-closed on parsed email: if angle-bracket email is present but not allowlisted, reject immediately — display-name markers (hermes, qwen, claude) never override. Fuzz cases in tests/test_git_attribution_guard.py (T3-B).  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_1171086a7740 -->
- oramaclaw pending conflicts cleared or orphaned by dead transactions must be archived to state_dir/registry/orphan-conflicts/ before removal — never silent delete (T3-C). sweep_orphan_pending runs on ControlStore.open.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_2abff9b4e522 -->
- Security and API fixes must trace the full data path from source to sink — a helper fix (e.g. _canonical_endpoint on return paths) does not protect import-time constants, module-level env reads, or unvalidated JSON consumers that bypass the helper.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_d9e84ea8c6c2 -->
- Dream-cycle salience scoring must compare UTC-aware timestamps — episodic entries use +00:00 offsets; naive datetime.now() minus aware fromisoformat raises TypeError and blocks auto_dream.py.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_65921fdf4329 -->
- PR descriptions and summaries are append-only historical records: never replace the original purpose with a follow-up job summary. When new work lands (CodeRabbit fixes, merge resolutions, CI notes), add a ## Follow-up section below the existing scope — same rule as LESSONS.md and integrative-merge doctrine.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_3b13ab0a45d4 -->
- Merge-conflict synthesize mode: when both branches changed the same region for valid different reasons, blend both — do not pick one side wholesale. Example PR #158: keep ipaddress-based _validate_endpoint_host from security-hardening AND locality/canonical-loopback tests from #157.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_257a631cbfd3 -->
- After a main history rewrite, never classify branches by merge-base exit 1 or ahead/behind counts alone — use scripts/git/reanchor_scan.sh plus git cherry -v; merge-base failure often means rewrite boundary not orphan work.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_154b34159790 -->
- Before destructive git ops (rebase all, branch delete, history surgery), append a markdown branch catalog to .agent/memory/working/BRANCH_CATALOG_COMPLETE_<date>.md using reanchor_scan output — read-only snapshot for triage.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_021fe0145c18 -->
- Git sticky routing before branch work: git-history-surgery SKILL for rewrite/reanchor; reanchor-after-rewrite.md for Case A/B twins; using-git-worktrees for worktree safety; windows-powershell-runtime-bootstrap before Win fetch/rebase/push; platform-line-endings-turf for .cmd CRLF (blocks macOS checkout).  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_aad58ebed148 -->
- RC-1 CommandCenter :33 empty-string fallback is sufficiently covered by commandCenterState.test.ts unit tests; full CommandCenter.test.tsx component test is optional when pure helpers are extracted and tested.  <!-- status=accepted confidence=0.55 evidence=1 id=lesson_706a71390405 -->
- When exploratory scratch PR (#117 vitest) and integration PR (#118 dev-recalib) overlap, fold scratch into integration or close scratch after merge — scratch branches can pass CI without exercising pnpm test if deps only exist on integration branch.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_817b824a0506 -->
- Vitest nav smoke tests: click Composer/Runs/Artifacts nav buttons and assert exclusive panel chrome — e.g. runs page has View All Runs but not Swarm Composer/Launch Swarm. File: web/src/features/command-center/CommandCenter.test.tsx.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_928c935b0607 -->
- Tracked skill files with literal workstation tree paths fail no-workstation-paths / LINT-006 on edit — use $(git rev-parse --show-toplevel) or repo-relative examples in using-git-worktrees and similar skills.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_a39da46c0708 -->
- Overlapping Windows CRLF turf PRs on platform/windows/gstack-brain-sync.cmd (#116, #117, #118) require coordinated merge order — land #116 (EOL normalize) before Vitest/dev-recalib integration or expect EOL conflict churn.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_b4aeb57d0809 -->
- Playwright E2E for orama web/ operator console is explicitly deferred until the Vitest RC-1 gate merges to main — tracked in docs/TDD.md post-gate backlog, not RC-1 minimum scope.  <!-- status=accepted confidence=0.55 evidence=1 id=lesson_c2c61b57583b -->
- After rewrite-boundary branch triage, dry cherry-pick onto current origin/main: if the patch applies empty or changes are already on main, delete the branch — do not open a PR (CI/pt71 cluster, routing branches session 2026-06-27).  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_c5bfa68e190a -->
- gbrain sync may block sync.last_commit on a single bad SKILL.md parse — use gbrain sync --skip-failed to acknowledge pre-existing failures and advance checkpoint after large orama doc imports.  <!-- status=accepted confidence=0.55 evidence=1 id=lesson_d3d72c68694a -->
- RC-1 Vitest operator-console gate (16 tests, 5 files, CI web-test job) is complete on feat/vitest-tdd-gate-scratch but not on origin/main until PR #118 merges — main web/package.json still has no vitest script.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_d6c0a79f2a1b -->
- Append a Progress status section to docs/testing/*.tdd.md evidence reports before merge — captures branch-vs-main state, open PRs, and backlog so triage survives branch cleanup.  <!-- status=accepted confidence=0.55 evidence=1 id=lesson_e4e83d796a5b -->
- TDD commit-msg gate: scripts/git/check_tdd_commit.sh blocks staged web/src production *.ts(x) without a staged *.test.ts(x) in the same commit unless the message contains tdd-skip: <reason>. Wired from .githooks/commit-msg via install-local-hooks.sh — not pre-commit (message unavailable there).  <!-- status=accepted confidence=0.65 evidence=1 id=lesson_e7d1b80a3b2c -->
- Ship related changes in logical commit batches for review: (1) tests, (2) hook/scripts, (3) canonical docs, (4) skill cross-refs — used on feat/vitest-tdd-gate-scratch 2026-06-27 (4 commits).  <!-- status=accepted confidence=0.55 evidence=1 id=lesson_f5f94e8a7b6c -->
- macOS ships bash 3.2 as /bin/bash — it lacks mapfile/readarray. scripts/git/*.sh hook scripts must use while IFS= read -r loops. Symptom: check_tdd_commit.sh exit 127 mapfile: command not found. Canonical ref: orama git-history-surgery/references/bash-32-git-script-portability.md.  <!-- status=accepted confidence=0.65 evidence=1 id=lesson_f8e2a91b4c3d -->
- oramaclaw pending conflicts cleared or orphaned by dead transactions must be archived to state_dir/registry/orphan-conflicts/ before removal — never silent delete (T3-C). sweep_orphan_pending runs on ControlStore.open.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_0a52069a7fa0 -->
- Block agentic-stack upstream Brain integration in .agent until brain_bridge.py supports both Gbrain and codejunkie99/brain — Gbrain via gstack is canonical for PT-orama RAG  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_144d7beb4018 -->
- ~/.gbrain/config.json must have a 'database_url' key explicitly — ~/.gbrain/.env is NOT auto-sourced in non-interactive shells (Claude Code agents, launchd background jobs). Export the URL from .env into config.json directly, or gbrain fails with 'database_url is missing from config' in every non-interactive context.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_163065104e2a -->
- SKILL.md YAML frontmatter must be the first bytes of the file — place lint-ignore HTML comments after the closing frontmatter delimiter  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_18dfc39b743f -->
- patch_models_yml must skip loopback win_ip the same way patch_devices_yml does — never persist localhost into shared YAML defaults  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_1bc5fdd8e247 -->
- PR descriptions and summaries are append-only historical records: never replace the original purpose with a follow-up job summary. When new work lands (CodeRabbit fixes, merge resolutions, CI notes), add a ## Follow-up section below the existing scope — same rule as LESSONS.md and integrative-merge doctrine.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_1f08ce9fa51a -->
- Pre-v2 security hardening (v1.1.1.0): all Linux-runnable tiers ship on cursor/security-hardening-pre-v2-c4ae in both repos; Mac/Win live E2E (start.sh, Ollama probes, LM_STUDIO_WIN_ENDPOINTS, hardware-policy harness, keychain) and T5 freeze tags stay blocked until tomorrow on real machines. Plan: orama-system/docs/plans/2026-06-27-security-hardening-pre-v2.md — platform schedule table marks each tier.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_27424f2d7fa7 -->
- When git pull is blocked by local commits, use git pull --rebase origin main. If the local commit content is already upstream (e.g. a duplicate episodic memory entry), rebase drops it silently with 'dropping...patch contents already upstream' — this is correct, not data loss.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_2856d7b3b7f3 -->
- Co-authored-by guards must fail-closed on parsed email: if angle-bracket email is present but not allowlisted, reject immediately — display-name markers (hermes, qwen, claude) never override. Fuzz cases in tests/test_git_attribution_guard.py (T3-B).  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_2aa89200cecf -->
- Hermes thin wrappers: four required (council, review, delegate, hardware-policy); lesson-mining is optional via --include-optional only — orama-system has no Perpetua-Tools dependency for graduation  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_3d15c25af434 -->
- After a main history rewrite, never classify branches by merge-base exit 1 or ahead/behind counts alone — use scripts/git/reanchor_scan.sh plus git cherry -v; merge-base failure often means rewrite boundary not orphan work.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_41d3bfca818a -->
- Before destructive git ops (rebase all, branch delete, history surgery), append a markdown branch catalog to .agent/memory/working/BRANCH_CATALOG_COMPLETE_<date>.md using reanchor_scan output — read-only snapshot for triage.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_4fab986d5cfc -->
- Merge-conflict synthesize mode: when both branches changed the same region for valid different reasons, blend both — do not pick one side wholesale. Example PR #158: keep ipaddress-based _validate_endpoint_host from security-hardening AND locality/canonical-loopback tests from #157.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_512dcbfb94de -->
- v2 oramasys/agate repo will own model_hardware_policy.yml schema, hardware_policy API, and hardware_policy_cli — perpetua-core imports agate; PT v1 paths are migration source only  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_5ca2112b2a5a -->
- LINT-006 flags OS-specific home-directory path forms and $HOME patterns even inside policy docs that WARN against them — use placeholder notation abstract home-directory placeholders or OS-specific home-directory path in ALL docs explaining what not to do.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_5f5ab082e311 -->
- Mac—”Win cross-harness E2E verification requires an actual curl probe: curl -s --max-time 5 http://${WIN_IP}:1234/v1/models — the start.sh --hardware-policy flag only validates openclaw.json config, it does NOT probe the live Win LM Studio endpoint.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_6077e426bb8c -->
- routing.json consumed by alphaclaw_bootstrap must pass jsonschema validation and RFC-1918 hostname allowlist before build_openclaw_config — never trust filesystem JSON without _validate_pt_state (T1-A).  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_60f9b192e2a0 -->
- setup_macos.py step_mac_agent_thinking must guard against string model shorthand — openclaw.json agents may use 'model': '<id>' (string) instead of {"primary": ...}. Guard with isinstance(_model, str): return before calling .get('primary'). Without guard: AttributeError crashes start.sh --status silently.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_61d63d446119 -->
- All .agent/memory writers must call sanitize_tracked_path_leaks() from path_hygiene.py before persisting strings — episodic log_execution, learn.py, graduate.py, and review queue renderer. Legacy rows: run scrub_memory_paths.py then re-render LESSONS.md; never hand-edit derived markdown to hide paths.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_700d472a2220 -->
- T5 tagging procedure: git tag v1.1.1 -m '<message>' then git push origin v1.1.1 in BOTH repos in the same session. No version bump needed if pyproject.toml/\_version.py already reflect the target version. Tag only after ALL E2E gates pass (Mac E2E + Win E2E + Mac—’Win cross-probe).  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_80a8b4101160 -->
- agentic-stack lives at vendor/agentic-stack in Perpetua-Tools like ecc-tools — orama start.sh symlinks lib/shared/agentic_stack from PT vendor path  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_8134a6cf1809 -->
- discover.py must split hash endpoints (LAN IP) from runtime endpoints (localhost on Windows) — never mutate endpoints win to LAN IP before patch_openclaw_json or lmstudio-win breaks  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_91f19059ee87 -->
- orama-system TDD gate (pre-commit check for adjacent *.test.ts(x) when web/src/ production files change) fires on merge resolution commits even when no new code was authored. Override with 'tdd-skip: <reason>' in the git commit message body — the commit-msg hook reads this line and bypasses the TDD requirement.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_a04a8dd3b911 -->
- Security and API fixes must trace the full data path from source to sink — a helper fix (e.g. _canonical_endpoint on return paths) does not protect import-time constants, module-level env reads, or unvalidated JSON consumers that bypass the helper.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_a19b581aee32 -->
- At Mac session start after a Win session, always read REVIEW_QUEUE.md FIRST — the Win session completes tasks that Mac thought were pending. Never repeat work the Win session already did.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_b194920de806 -->
- After vendor/agentic-stack submodule init run install-agentic-stack.sh then agentic-stack upgrade --dry-run before harmonizing PT .agent/ — union-merge only, never commit into vendor/  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_be2a10129cad -->
- When adding Windows .cmd shims in Python always write CRLF bytes and add gitattributes eol=crlf — LF-only batch files fail silently under cmd.exe per PR #108 gstack-brain-sync.cmd incident  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_cea128a023a9 -->
- Dream-cycle salience scoring must compare UTC-aware timestamps — episodic entries use +00:00 offsets; naive datetime.now() minus aware fromisoformat raises TypeError and blocks auto_dream.py.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_e3680054a078 -->
- xAI Grok Build (Elon Musk) installs a bare agent CLI at ~/.grok/bin/agent — it is NOT Cursor. PATH collision: scripts invoking agent without a path hit Grok TUI instead of cursor-agent. Always call cursor-agent explicitly and document both binaries in harness skills.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_e9b8b2213874 -->
- setup_macos.py step_mac_agent_thinking must guard against string model shorthand — openclaw.json agents may use 'model': '<id>' (string) instead of {"primary": ...}. Guard with isinstance(_model, str): return before calling .get('primary'). Without guard: AttributeError crashes start.sh --status silently.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_10620fe7285b -->
- ~/.gbrain/config.json must have a 'database_url' key explicitly — ~/.gbrain/.env is NOT auto-sourced in non-interactive shells (Claude Code agents, launchd background jobs). Export the URL from .env into config.json directly, or gbrain fails with 'database_url is missing from config' in every non-interactive context.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_caabfb16dfa1 -->
- git add --renormalize with 'text eol=crlf' in .gitattributes can store raw CRLF bytes in the blob on macOS, creating a phantom git diff where cmp shows working-tree and HEAD blob are byte-identical yet git diff HEAD shows 69+ lines changed. Fix: write LF content to file, git add (normalizes to LF blob), commit locally, then git pull --rebase drops it as 'patch already upstream'.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_37ed595a3b57 -->
- orama-system TDD gate (pre-commit check for adjacent *.test.ts(x) when web/src/ production files change) fires on merge resolution commits even when no new code was authored. Override with 'tdd-skip: <reason>' in the git commit message body — the commit-msg hook reads this line and bypasses the TDD requirement.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_47847426bd94 -->
- PR landing in orama-system and Perpetua-Tools always uses squash-merge (gh pr merge --squash). When a PR shows CONFLICTING/DIRTY on GitHub: (1) git merge origin/main locally, (2) resolve conflicts additively (union both sides), (3) push branch, (4) gh pr merge --squash. Never regular merge commit; never rebase-merge. squash keeps main history linear.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_001158f408c1 -->
- SSRF validation must convert IPv4-mapped IPv6 addresses before checking is_link_local: if isinstance(addr, ipaddress.IPv6Address) and addr.ipv4_mapped is not None: addr = addr.ipv4_mapped. Without this, ::ffff:169.254.x.x passes ipv6.is_link_local=False check even though the underlying IPv4 address is link-local. Fixed in PT PR #154 alphaclaw_bootstrap.py.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_a88a10dfcba5 -->
- LAN peer WS+SSE state machine: WS_CONNECTING (5s) -> WS_CONNECTED | fail(x2) -> SSE_CONNECTING -> SSE_CONNECTED | DISCONNECTED (30s retry -> WS). Heartbeat every 15s. Two consecutive WS failures demote to SSE before retry cycle. Transport abstraction = only lan_peer_channel.py knows which wire is live.  <!-- status=accepted confidence=0.9 evidence=1 id=lesson_legacy_8670b3569ccd -->
- PT memory pipeline (promote.py, auto_dream.py, and siblings) must use open(path, encoding="utf-8") for every .md and .jsonl read on Windows. The default cp1252 codec silently works until LESSONS.md gains a non-ASCII character (em-dash, smart quote, arrow) — then crashes with UnicodeDecodeError on byte 0x9D. Fixed in PT 0ed5635. Audit all open() calls in .agent/memory/*.py.  <!-- status=accepted confidence=0.95 evidence=2 id=lesson_legacy_8a8f1dbdd852 -->
- LAN P2P bidirectional transport (2-host Python/FastAPI): use FastAPI WebSocket as primary (zero new deps, dual-socket, ~40 LoC) with SSE+POST as HTTP-only fallback (also zero deps, 2 connections). Channel manager (lan_peer_channel.py) abstracts both transports behind send()/on_inbound(). Shared JSON envelope {type, source, ts, data} makes transport invisible to callers.  <!-- status=accepted confidence=0.9 evidence=1 id=lesson_legacy_e0fb9321d79f -->
- macOS Keychain service names for OpenClaw: 'openclaw.gemini-main-apikey' (->OPENCLAW_MODELS_PROVIDERS_GEMINI_MAIN_APIKEY), 'openclaw.gemini-fallback-apikey' (->OPENCLAW_MODELS_PROVIDERS_GEMINI_FALLBACK_APIKEY), 'openclaw.telegram-bot-token' (->OPENCLAW_TELEGRAM_BOT_TOKEN), 'openclaw.gateway-auth-token' (->OPENCLAW_GATEWAY_AUTH_TOKEN). Store: printf '%s' VALUE | bash $OPENCLAW_ROOT/orama-system/scripts/openclaw/store_keychain_secret.sh <service>. Load at startup: source $OPENCLAW_ROOT/orama-system/scripts/openclaw/load_keychain_secrets.sh  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_2a6766b489c5 -->
- Always serialize timestamps in UTC to avoid cross-region comparison bugs  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_74d824bb84e2 -->
- Portal peer-file 404: pull orama 86c90bc+, restart --lan-peer; peer-stream needs response_class=StreamingResponse  <!-- status=accepted confidence=0.9 evidence=1 id=lesson_legacy_peerstream_20260628 -->
- Post-commit sync: git fetch --prune, pull --rebase origin main, push, verify HEAD equals origin/main in both repos  <!-- status=accepted confidence=0.9 evidence=1 id=lesson_legacy_sync_rebase_20260628 -->
- LM Studio loads only one model per instance; probe state=loaded via /api/v0/models before dispatch  <!-- status=accepted confidence=0.59 evidence=1 id=lesson_712a5068a678 -->
- Windows partner CLIs use parametric LOCALAPPDATA and USERPROFILE paths; bootstrap with ensure-partner-cli-paths.ps1  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_5d96448dd529 -->
- Codex CLI v0.142.x uses three dispatch profiles: fanout (codex exec -C repo -s workspace-write --dangerously-bypass-approvals-and-sandbox), bounded (exec workspace-write only), interactive (--sandbox danger-full-access --ask-for-approval never). Legacy --approval-mode removed in 0.140+. Resolve repo root and Codex binary at runtime; never hardcode host paths in prompts.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_2cef6113c1f1 -->
- Hermes skill absorption: redirect stubs and .agents wrappers stay thin; canonical bodies live only in bin/orama-system/skills. perpetua-hardware in .agents targets hardware-affinity-gate (orama methodology); PT runtime SSoT stays in Perpetua-Tools policy YAML. Archive llm-council-orchestration is SUPERSEDED by pt-orama-council; Win LM Studio coder is qwen3.5-27b-claude-4.6-opus-reasoning-distilled-v2 not invented Qwen 3.6 Coder.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_ed634fbe6cd0 -->
- Hermes thin wrappers: four required (council, review, delegate, hardware-policy); lesson-mining is optional via --include-optional only — orama-system has no Perpetua-Tools dependency for graduation  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_6ce521f323f4 -->
- Hermes dispatch envelope: L3 skill_id+args+agent_id; L2 adds orama_system_root, executor_id, optional transport partner profile; L0 OpenClaw core result plus optional Hermes extensions  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_f6de10a70a81 -->
- ~~LINT-006 flags OS-specific home-directory path forms and $HOME patterns even inside policy docs that WARN against them — use placeholder notation abstract home-directory placeholders or OS-specific home-directory path in ALL docs explaining what not to do.~~  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_8c6e3368a308 superseded_by=lesson_eb4a1cb51115 -->
- Mac↔Win cross-harness E2E verification requires an actual curl probe: curl -s --max-time 5 http://${WIN_IP}:1234/v1/models — the start.sh --hardware-policy flag only validates openclaw.json config, it does NOT probe the live Win LM Studio endpoint.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_bd8505031496 -->
- At Mac session start after a Win session, always read REVIEW_QUEUE.md FIRST — the Win session completes tasks that Mac thought were pending. Never repeat work the Win session already did.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_20586d86a285 -->
- When git pull is blocked by local commits, use git pull --rebase origin main. If the local commit content is already upstream (e.g. a duplicate episodic memory entry), rebase drops it silently with 'dropping...patch contents already upstream' — this is correct, not data loss.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_66d2f9a8432a -->
- T5 tagging procedure: git tag v1.1.1 -m '<message>' then git push origin v1.1.1 in BOTH repos in the same session. No version bump needed if pyproject.toml/\_version.py already reflect the target version. Tag only after ALL E2E gates pass (Mac E2E + Win E2E + Mac→Win cross-probe).  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_601a930aa10d -->
- When git commit output is piped through '| tail -N', a commit failure can look like success because the error message is truncated. Always check 'git status --short' after a commit that used tail-truncated output to confirm the file is no longer staged.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_18f5ca481f90 -->
- Mac and Win orama-system installs self-talk over LAN via HTTP only: peer IP from last_discovery.json, inference via LM Studio /v1/models, control-plane via portal /health and /api/status after PORTAL_BIND_LAN=1 and shared ORAMA_CONTROL_PLANE_TOKEN on both hosts — use probe_lan_peer.py or Hermes /lan-peer-self-talk; no SSH and no second RPC layer  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_12ddf8cf63b9 -->
- Windows start.ps1 must parse bash-style flags from args (--no-open --stop --status) because PowerShell switch params reject --kebab-case; never assign to dollar-pid — use listenerPid — PID is a read-only automatic variable  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_0d5d6b4a25eb -->
- Windows orama uvicorn launch must match start.sh: orama_system.api_server and portal_server modules with PYTHONPATH repo src and root — bare api_server colon app fails silently  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_52add7792e48 -->
- PowerShell ProcessStartInfo EnvironmentVariables is a StringDictionary — use ContainsKey not Contains or Start-Service throws before uvicorn launches  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_51f853af60eb -->
- After start.ps1 rehab on Windows verify PT 8000 orama 8001 Portal 8002 UP via start.ps1 --status before LAN peer probe  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_998b0a438dd4 -->
- Windows LAN peer self-talk succeeds when .env.local sets PORTAL_BIND_LAN=1 and ORAMA_BIND_LAN=1, start.ps1 is launched via powershell.exe -File with LF line endings, and last_discovery.json is current  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_2e8c4ea72453 -->
- NEVER run git clean -fd or git clean -fdx without backup or stash  <!-- status=accepted confidence=0.425 evidence=1 id=lesson_d66d6afdaefb -->
- If the only goal is 'worktree matches remote', use git restore --staged --worktree . and manually remove confirmed files; do not destroy untracked files first  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_23e3d608b63e -->
- LAN peer co-orchestration uses markdown file drops via lan_peer_assign.py and POST /api/peer-file to ~/.openclaw/state/lan_peer/inbox/. Fan-out manifest JSON splits topics by assignee mac or win. WS and SSE are for heartbeat and probe only; large autoresearch payloads are plain files.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_87636d658879 -->
- lan_peer_assign.py resolves orama repo root as Path(__file__).resolve().parents[4], not parents[5]. Local inbox import requires orama_system on sys.path from repo/src. parents[5] pointed at OpenClaw parent and caused ModuleNotFoundError.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_cbcd146eb431 -->
- lan_peer_assign fanout continues on per-item peer drop failures and returns status partial with exit code 1 when any remote drop fails while local assignments succeed. Mac can proceed while Win catches up on git pull and portal restart.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_46944e3665db -->
- On Win, Mac-to-Win assignments arrive in the local lan_peer inbox. Win operators read inbound files with lan_peer_assign.py list and read --name without --peer. Inbound peer files are already local after Mac drop --peer.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_206dc2c869c3 -->
- Win co-orchestrator replies use lan_peer_assign.py drop --peer --file with assignee mac. Mac reads Win deliverables via list --peer and read --peer --name. Standard deliverables: gpu-results.md, win-code-review.md, win-self-improve-runtime-results.md.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_dc67a19abdf7 -->
- ~~autoresearch_bridge.py sync_autoresearch_idempotent uses SSH to GPU_BOX by default. LAN co-orchestration on Win uses HTTP LM Studio and file inbox instead. Local Win preflight must not block on SSH timeout when the operator runs on the Win host.~~  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_7791e7860857 superseded_by=lesson_aeb3cd01c203 -->
- Mac must git pull orama-system main at or after 9f89051 and restart with ./start.sh --lan-peer --no-open before /api/peer-file accepts Win drops. portal-health can PASS while peer-file returns 404 on an old portal process.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_130073d9e30a -->
- LAN peer joint auth mode accepts PT lane token from Perpetua-Tools .state/control_plane_token plus orama env lanes. probe_lan_peer.py reports auth_mode joint when both lanes are configured; either key unlocks portal-status on probes.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_43c39af8176f -->
- On Mac when Ollama is warm and LM Studio is passive-only, route Mac co-orchestrator and subagent inference through ollama-mac at localhost:11434. LM Studio :1234 may still answer peer-lmstudio probes with a model catalog but must not be the primary execution path unless hardware policy requires MLX on that port.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_49a5af119f6f -->
- Cross-repo model-endpoint-policy parity CI on pull_request must checkout the sibling repository at github.head_ref (same branch name), not main — stacked security PRs diverge from main until merge; rm -rf sibling dir before main fallback to avoid partial checkout.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_4b3d787319a1 -->
- Cross-repo Python policy parity scripts must compare function ASTs with ast.dump(), not ast.unparse() — quote-style and formatting differences cause false diverge failures even when logic is identical.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_c97cfd0679bb -->
- validate_model_endpoint_url must catch urlparse().port ValueError on malformed ports and re-raise ModelEndpointPolicyError — uncaught ValueError becomes HTTP 500 instead of policy rejection.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_816433497c31 -->
- Control plane token persistence must use _secure_write_token with os.open O_CREAT and mode 0600 — never plain open().write() for secrets on disk.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_67a7e561207f -->
- GET /health must validate MODEL_ENDPOINT_URL through validate_model_endpoint_url before any outbound probe — blocks SSRF when env injection supplies a non-loopback URL.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_1d3b8cae0eef -->
- POST /runtime/bootstrap logging must pass payloads through redact_runtime_payload before emission — never log raw bootstrap bodies that may contain tokens or secrets.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_fb42336ab528 -->
- Portal lifecycle mutating routes (stop, restart, rediscover) require same-origin Origin or Referer guards in addition to bearer token — CSRF protection is mandatory; session cookies are optional UX only per SECURITY.md.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_3e31f4040c55 -->
- Stacked cross-repo security PRs may intentionally share the same branch name (e.g. cursor/security-pr1-pr2-auth-hardening-f559) — parity CI head_ref checkout is what binds sibling repos, not branch-name coincidence alone.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_c935b0102fb9 -->
- Tests and docs must not embed $HOME/ or other workstation-specific absolute paths — repo_hygiene LINT-006 blocks them; use <local-temp> placeholders or repo-relative paths in fixtures.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_cc6cbd625069 -->
- Portal HTML rendering must escape all user-controlled strings with html.escape (or equivalent _tag helper) before embedding in templates — never interpolate raw query params or flash messages into HTML.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_94097ce6f710 -->
- After a PR merges, always verify origin/main contains every commit on the feature branch — unattended merges and continued pushes to the same branch name leave post-merge commits orphaned until a follow-up PR is opened.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_0425d3219be5 -->
- Do not assume a branch tip is merged just because its PR shows MERGED — GitHub merges the PR snapshot at merge time; agents that push after merge create a divergent branch tip that main never receives.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_005241c94fc6 -->
- When multiple AI agents share a branch name across repos or sessions, treat merge time as a hard boundary — scan git log origin/main..origin/<branch> on both repos before closing work, and open a fresh follow-up branch/PR for any commits landed after merge.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_ff3b885b1eae -->
- When ws-peer probe PASS is bidirectional between Mac and Win portal, L3 file inbox co-orchestration is fully operational. Mac uses OpenClaw plus Ollama warm; Win uses Hermes plus LM Studio 27B. Coordinate only via lan_peer_assign file drops, not remote agent RPC.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_20833366511b -->
- Windows Portal http://localhost:8002/ Internal Server Error on dashboard root: pull orama-system main at or after 435d27a and restart start.ps1 --lan-peer. Fix unwraps redacted agents dict in portal_server _render_html; /health may PASS while / returns 500 on stale portal process.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_78f4700bd60c -->
- Portal / returned HTTP 500 after restart if portal_server is not updated first: rerun start.ps1 --lan-peer after pulling latest origin/main  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_64dedfe61cfa -->
- Rebase or cherry-pick post-merge orphan PRs onto current main before closing or merging: first-commit conflicts with main usually mean the work already landed via a different PR path.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_30de39cf9ada -->
- Draft PR triage: three-dot diff inflates post-merge orphan PRs; always compare two-dot at branch tip vs main and cherry-pick net-new commits instead of merging stale multi-hundred-commit-behind branches.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_5222ac71c9ef -->
- active_tilting Win Ollama models must resolve to port 11434 not lm-studio :1234: detect_active_tilting_ip returns the LM Studio endpoint; ModelRegistry._resolve_host must branch on backend=ollama and rebuild http://hostname:port from models.yml.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_50e33b4bf1ec -->
- Co-orchestration mutations use subagent branches named subagent/role/topic from origin/main; coordination stays on main via file inbox.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_ee24710b4c2f -->
- Win LAN peer inbox canonical URL is /peer-inbox served from platform/windows/peer_inbox_portal.py. /co-orchestration/windows and /co-orchestration on Win hosts 307 redirect to /peer-inbox. Hermes co_orchestration_windows skin removed.  <!-- status=accepted confidence=0.65 evidence=1 id=lesson_0762f924239d -->
- When parallel Task subagents hit harness usage limits, parent agent executes the assignment inline on the same host and still drops lan_peer_assign deliverables to the peer inbox. Do not block co-orchestration on subagent quota.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_0ec02977f23a -->
- autoresearch_bridge preflight AUTORESEARCH_PREFLIGHT_MODE auto uses http-local when GPU_BOX host is loopback or local IP: local git sync in LOCAL_REPO_PATH and HTTP GET LM Studio /v1/models. SSH path preserved for Mac-to-Win remote. Avoids 90s SSH timeout blocking Win co-orchestration.  <!-- status=accepted confidence=0.65 evidence=1 id=lesson_aeb3cd01c203 -->
- H5 autoresearch GPU harness run_h5_gpu_benchmark.py runs three rubric coding prompts on Win LM Studio 27B with max five iterations each. Drop gpu-results-h5.md to Mac peer inbox. Mac Ollama 9B parallel leg required to close cross-host comparison.  <!-- status=accepted confidence=0.65 evidence=1 id=lesson_b7fb9002c24f -->
- Graceful degradation uses stop-at-first-answer ladders: oramasys search gbrain to CRG to Grep to web; PT inference host-local to validated fallback to cloud budget cutoff; LAN ws-peer to SSE and file inbox partial fan-out; autoresearch http-local preflight before SSH. Document the fallback tier in one line when escalating.  <!-- status=accepted confidence=0.65 evidence=1 id=lesson_c8dc70c59ac9 -->
- Co-orchestration cycle mutations use subagent branches named subagent/role/short-topic branched from origin/main. Push with git push -u origin branch. Coordination and fan-out manifests stay on main via lan_peer_assign file inbox. Operator opens PR per branch; no force-push.  <!-- status=accepted confidence=0.65 evidence=1 id=lesson_d0dfe41fb420 -->
- Mac orchestrator drops one Win subagent assignment per role per coord round; autoresearcher before coder; Win serializes via win_job_queue.py with one active job per role and single-tenant LM Studio.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_4c89202aa915 -->
- After each LAN coord round both Mac and Win run PT learn.py for session lessons and auto_dream.py to stage patterns; commit and push .agent/memory before the next fan-out.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_da75fcd087d6 -->
- LAN co-orchestration cycles honor v1.1 frugality tiers 0-2 local-first per docs/plans/2026-05-29-03-v1.1-definitive.md; state escalation tier in one line; no new architecture beyond existing graceful-degradation ladders.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_fe81ad58a7a1 -->
- PowerShell ops scripts under orama-system must use ASCII-only punctuation in string literals; Unicode em-dash or smart quotes cause ParserError on Windows PowerShell 5.x and break background monitors like coord_monitor.ps1.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_c391481ca104 -->
- When Mac and Win both push Perpetua-Tools .agent/memory in the same coord round, git pull may conflict on lessons.jsonl and LESSONS.md; union-merge all lesson rows from both sides and never drop Mac learn.py rows or Win operator-approve rows.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_8b5d45070494 -->
- H5 finalize Win autoresearcher job pattern: git pull Mac results, read mac-h5-comparison.md, synthesis-only update gpu-results-h5-cross.md, drop gpu-results-h5-final.md to peer, win_job_queue complete autoresearcher without GPU re-run (B1 frugality).  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_82ab64772b2b -->
- coord_monitor.ps1 during active cycles validates lesson_9b3d7e2f41ac: probe plus enqueue detected coord-005 win-autoresearcher-h5-finalize.md at monitor tick 5 before manual operator poll.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_2a476c761ca1 -->
- H3 autoresearch hypothesis falsified for trivial prompts: Win 27B wall clock about 10 seconds on trivial prompt class versus faster Mac 9B MLX. Route by task class in routing.yml — quality-heavy and autoresearch-coder to win-rtx3080; latency-sensitive probes and orchestrator chatter to Mac Ollama. Affinity is not a universal speed win.  <!-- status=accepted confidence=0.65 evidence=1 id=lesson_1f9c927792ba -->
- Co-orchestrator portal monitor URLs: Mac operators use /co-orchestration/macos on port 8002; Win operators use /peer-inbox. Legacy /co-orchestration/windows redirects to /peer-inbox on Win hosts.  <!-- status=accepted confidence=0.65 evidence=1 id=lesson_203c342c1e85 -->
- Bridge PR verify coder job: checkout subagent/win-coder/bridge-http-local, run tests/test_autoresearch_bridge.py (expect 38 pass), document live preflight degraded if ollama CLI missing, drop win-bridge-pr-ready.md before win_job_queue complete coder.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_7fc75916a601 -->
- Graceful degradation Ladder F adds model-routing-check dispatch gate: endpoint reachability, hardware_policy_cli affinity fail-closed, routing.yml cross-check, degraded single-endpoint continue per perpetua-model-routing-check.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_81a9b9806526 -->
- When coord queues idle over 15 minutes with no new inbox cards, resume V1_DEFERRED_BACKLOG: operator PRs, P1 frugality_router, and v1.1 plan section 9 out-of-scope items.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_7588896135cf -->
- On Mac peer portal-health timeout during coord monitor, Win continues local queue processing and retries lan_peer_assign drop when probe returns green; do not block coder or autoresearcher on peer outage.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_b6d64dcb2d7f -->
- ~~Mac H4 comparison complete: Ollama 9B warm clamp about 20.2s versus Win LM Studio 27B about 33.1s. Win H5 harness passed 3/3 rubric tasks on first iteration. Cross-host synthesis in gpu-results-h5-cross.md; Mac H5 Ollama leg pending mac-h5-comparison.md.~~  <!-- status=accepted confidence=0.65 evidence=1 id=lesson_7d2c1e8f5b90 superseded_by=lesson_e2f8a41c7d93 -->
- ~~Win co-orchestrator processes Mac assignment cards sequentially via win_job_queue.py: routes win-autoresearcher-* and win-coder-* inbox files to separate role queues with at most one active job per role. State file lan_peer/win_job_queue.json. Run enqueue then run-once or next role then complete role.~~  <!-- status=accepted confidence=0.65 evidence=1 id=lesson_a3f8e2b91c04 superseded_by=lesson_c6e4f1a89d20 -->
- At end of each coord monitor window git fetch and pull --rebase origin main on both orama-system and Perpetua-Tools then push before reporting session complete; Win may land lessons and results while Mac monitors.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_1cbdd2917d26 -->
- Graceful degradation Ladders A through F in oramasys-method references graceful-degradation.md are SSOT; PT perpetua-model-selection tiers 0-6 map to Ladder B; working cards hold pointers only not duplicate ladder prose.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_e38573d65b51 -->
- During active coord cycles poll every 2-3 minutes: probe_lan_peer.py --json, lan_peer_assign.py list and list --peer for new win-autoresearcher-* or win-coder-* cards, win_job_queue.py enqueue and status. Mac H5 leg mac-h5-comparison.md remains Mac-owned until dropped to Win peer inbox.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_9b3d7e2f41ac -->
- win_job_queue.py prune removes non-actionable pending jobs (mac-* deliverables, hypothesis summaries, ops noise) on every enqueue. Use complete-pending role job_id to reconcile already-finished coord jobs without re-running GPU or coder spikes. coord-003 jobs reconciled after branch deliverables landed.  <!-- status=accepted confidence=0.65 evidence=1 id=lesson_c6e4f1a89d20 -->
- H5 cross-host closed coord-005: Mac Ollama 9B 3/3 pass at iterations 1/4/5 (490s wall) versus Win LM Studio 27B 3/3 at 1/1/1 (280s wall). Win wins iterations-to-pass and wall on all rubric tasks. Final synthesis in gpu-results-h5-final.md; routing keeps autoresearch-coder on Win 27B.  <!-- status=accepted confidence=0.7 evidence=1 id=lesson_e2f8a41c7d93 -->
- **Perpetua-Tools** = `REPO_ROOT\Downloads\SKILLS.md\uLtrathink\Perplexity-Tools`  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_56484b597a48 -->
- **orama-system** = `REPO_ROOT\Downloads\SKILLS.md\uLtrathink\ultrathink-system`  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_7ce91370c684 -->
- When Mac merges a PT PR during Win coord_monitor listen window, pull main immediately and run combined frugality plus bridge test suites before dropping merge-ack to Mac peer.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_899c59b7cf65 -->
- Triple rinse cycle: execute queue job, learn from PT agent, auto_dream, push sync, then coord_pulse dry-run when idle repeats Mac Win coord loop.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_69e096bf1420 -->
- DO NOT assume directory or structure names: when the user gives an explicit name (e.g. '.agent/memory'), use it verbatim — never silently 'correct' it to a guess (e.g. '.agents/memory'). If it seems wrong or you have not read its conventions, STOP and ASK first. Read the area's AGENTS.md/_index before writing.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_65481a3bcd13 -->
- Workspace doxxing antipattern: never graduate or echo lessons that pin %USERPROFILE% or Downloads/SKILLS.md trees as canonical workspace — use orama-system + Perpetua-Tools repo names and REPO_ROOT/PERPETUA_TOOLS_ROOT env anchors; sanitize_tracked_path_leaks now maps workspace trees to <workspace-root>  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_864179d7e109 -->
- win_job_queue.py prune removes non-actionable pending jobs (mac-* deliverables, hypothesis summaries, ops noise) on every enqueue. Use complete-pending role job_id to reconcile already-finished coord jobs without re-running GPU or coder spikes. coord-003 jobs reconciled after branch deliverables landed.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_006c1d76348b -->
- coord_pulse returns idle when win_job_queue pending=0 even if lan_peer inbox has new markdown cards — inbox files are not jobs until enqueued or pulse-gate marks them actionable  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_080b3c99d237 -->
- Graceful degradation uses stop-at-first-answer ladders: oramasys search gbrain to CRG to Grep to web; PT inference host-local to validated fallback to cloud budget cutoff; LAN ws-peer to SSE and file inbox partial fan-out; autoresearch http-local preflight before SSH. Document the fallback tier in one line when escalating.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_08949e2ee3a0 -->
- After each coder queue job completes, run coord_monitor 15 minutes then Mac peer probe, push sync, and batch learn.py before second push.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_09145f182ba7 -->
- H5 finalize Win autoresearcher job pattern: git pull Mac results, read mac-h5-comparison.md, synthesis-only update gpu-results-h5-cross.md, drop gpu-results-h5-final.md to peer, win_job_queue complete autoresearcher without GPU re-run (B1 frugality).  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_0b9d387f40fb -->
- Win bucket drain ack win-bucket-drain-ack.md signals coord-005 backlog cleared; next Mac job from V1_DEFERRED_BACKLOG is P1 frugality_router then operator PR triage PT 183.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_0c1a389695f9 -->
- When Win queue is drained post coord-023, operator must fan out new win-coder-* and win-autoresearcher-* assignment cards to local inbox and win_job_queue.py enqueue before pulse can dispatch jobs  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_0c962e7f2dc9 -->
- Before git pull when Perpetua-Tools is behind origin stash local candidate and submodule drift then pop or checkout --theirs on conflict; Mac may land 20+ learn.py rows while Win idle.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_0e7b8ae4c70b -->
- G1 frugality tier ratio stays not measurable until frugality-report script and v1_1 session harness ship even after frugality_router merges on PT main.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_0ee71a86cdb9 -->
- Do not assume a branch tip is merged just because its PR shows MERGED — GitHub merges the PR snapshot at merge time; agents that push after merge create a divergent branch tip that main never receives.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_0fd89052c1bc -->
- Win job queue roles are coder and autoresearcher; Mac mac_job_queue roles are orchestrator and researcher mirroring the same one-active-per-role serial pattern.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_12912b1509b3 -->
- P5 T1 sign_operator_payload embeds expires_at inside HMAC canonical JSON; verify uses secrets.compare_digest and rejects expired tokens.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_130d7d1bc93c -->
- L1 preview is mandatory for fan-out two or more jobs and all HITL-class launches; single read-only Codex may stay bearer-only fast-path like dispatch_codex_partner.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_155edbbfda51 -->
- Cross-repo model-endpoint-policy parity CI on pull_request must checkout the sibling repository at github.head_ref (same branch name), not main — stacked security PRs diverge from main until merge; rm -rf sibling dir before main fallback to avoid partial checkout.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_17b9b00f429f -->
- During active coord cycles poll every 2-3 minutes: probe_lan_peer.py --json, lan_peer_assign.py list and list --peer for new win-autoresearcher-* or win-coder-* cards, win_job_queue.py enqueue and status. Mac H5 leg mac-h5-comparison.md remains Mac-owned until dropped to Win peer inbox.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_19f7e6abc435 -->
- Lock P5 decisions in docs/plans/P5-DECISIONS-LOCKED.md on implementation branch; amend via explicit PR note not silent drift.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_1a2b8466e8da -->
- Researcher GPU backlog after H5: next cycle is preflight/doc spike (h6) on subagent/win-autoresearcher/researcher-backlog-h6 unless Mac drops new hypothesis card  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_1bb7992e2130 -->
- Mac orchestrator drops one Win subagent assignment per role per coord round; autoresearcher before coder; Win serializes via win_job_queue.py with one active job per role and single-tenant LM Studio.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_1c497c2651d5 -->
- validate_model_endpoint_url must catch urlparse().port ValueError on malformed ports and re-raise ModelEndpointPolicyError — uncaught ValueError becomes HTTP 500 instead of policy rejection.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_21806a19d221 -->
- Researcher backlog handoff after H5: Win autoresearcher completes doc-spike preflight (gpu-results-h6-preflight.md), drops to Mac peer, and blocks next GPU harness until Mac researcher fans out a new hypothesis card with falsification criteria  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_22215380c3c5 -->
- Rebase or cherry-pick post-merge orphan PRs onto current main before closing or merging: first-commit conflicts with main usually mean the work already landed via a different PR path.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_2825e171dd34 -->
- Co-orchestration cycle mutations use subagent branches named subagent/role/short-topic branched from origin/main. Push with git push -u origin branch. Coordination and fan-out manifests stay on main via lan_peer_assign file inbox. Operator opens PR per branch; no force-push.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_2852ff537f20 -->
- P5 T2 next: api_swarm_preview returns preview_id approval_token expires_at signed via sign_operator_payload with assignments_hash in canonical payload.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_29aad50850da -->
- Win queue idle after job burst is expected; synthesize win-coder cards from Mac inbox intel when orchestrator has backlog.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_2dde2956ef3d -->
- Bridge PR verify coder job: checkout subagent/win-coder/bridge-http-local, run tests/test_autoresearch_bridge.py (expect 38 pass), document live preflight degraded if ollama CLI missing, drop win-bridge-pr-ready.md before win_job_queue complete coder.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_2fda473cc2dd -->
- L1 killswitch is tiered: POST /api/l1/stop terminates registered child PIDs only; full POST /api/stop NUCLEAR stack runs only on explicit Stop All Emergency control.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_3078938d375f -->
- When parallel Task subagents hit harness usage limits, parent agent executes the assignment inline on the same host and still drops lan_peer_assign deliverables to the peer inbox. Do not block co-orchestration on subagent quota.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_319a924869c4 -->
- Parse bold markdown Priority fields in assignment cards when computing queue sort order  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_32a019a0e432 -->
- mac-coord-023-queue-ack reconciled 18 Win inbox drops on Mac; remaining Mac work is mac-orchestrator-self-improve-003 and researcher GPU backlog — Win queue empty is expected post-drain  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_331ae3238c8d -->
- Control plane token persistence must use _secure_write_token with os.open O_CREAT and mode 0600 — never plain open().write() for secrets on disk.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_366db0125868 -->
- When multiple AI agents share a branch name across repos or sessions, treat merge time as a hard boundary — scan git log origin/main..origin/<branch> on both repos before closing work, and open a fresh follow-up branch/PR for any commits landed after merge.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_385c1ef845dd -->
- PT .agent memory commits belong on main only; subagent PR branches must contain code-only diffs — cherry-pick memory to main and reset spike branch to c7161de before opening PR.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_3b08348ab886 -->
- Pulse unified coord-019: both hosts use win_job_queue and mac_job_queue pulse-gate, last_pulse_seen inbox diff, BLOCKED_PENDING in Python only; Win keeps role-specific agent cards and coord_monitor listen; Mac keeps flock lock and job_cycle_listen reset-on-job.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_3dad46774ec3 -->
- autoresearch_bridge preflight AUTORESEARCH_PREFLIGHT_MODE auto uses http-local when GPU_BOX host is loopback or local IP: local git sync in LOCAL_REPO_PATH and HTTP GET LM Studio /v1/models. SSH path preserved for Mac-to-Win remote. Avoids 90s SSH timeout blocking Win co-orchestration.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_3e5b05d503d9 -->
- P5 closes client approved bool not human review: HMAC proves preview endpoint ran; stolen bearer can still auto preview-launch within TTL.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_433a6636156f -->
- Stacked cross-repo security PRs may intentionally share the same branch name (e.g. cursor/security-pr1-pr2-auth-hardening-f559) — parity CI head_ref checkout is what binds sibling repos, not branch-name coincidence alone.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_43ad5b16bcbb -->
- After T1 merge on branch, l1_dispatch _p5_landed import succeeds but CLI still blocked until portal /api/l1/* wired in T2-T7.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_468ca5b37bdd -->
- Anti-doxxing must be wired at every layer: protocols/path-hygiene.md, AGENTS.md, permissions.md never-rule, path_hygiene.py sanitize, Cursor alwaysApply rules in PT and orama, and repo_hygiene CI — not just LESSONS bullets  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_4955d81f9c97 -->
- P1 frugality_router.py spike lands on subagent/mac-orchestrator/frugality-router-spike with resolve_route tier 0-2 first, ORAMASYS_OFFLINE rejects tier>=3, privacy_critical forbids tier>=4, and ot.tool.tier JSONL traces; G1 frugality-report harness remains follow-on per v1.1 plan section 11.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_4b15ae2422ce -->
- P5 swarm HITL on cursor/security-pr3-swarm-approval-f559: T1 operator payload + T2 preview signing done (2/7); T3 launch token verify is next; L1 /api/l1/* stays blocked until PR merges  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_4bf720935b1d -->
- After Mac security PR auth hardening peer-file returns HTTP 401 until Win portal restarts with start.ps1 --lan-peer so joint control plane token resolves; retry drops after probe green.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_574ba3ae6784 -->
- Blocked prerequisite jobs stay in win_job_queue pending but coord_pulse.ps1 skips them via BlockedPending list until operator unblocks.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_5ab6535ac716 -->
- Defer future autoplan features as win-coder inbox cards with explicit prerequisite gates and low queue priority so coord cycles stay first.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_5c0ddfb6bd51 -->
- GET /health must validate MODEL_ENDPOINT_URL through validate_model_endpoint_url before any outbound probe — blocks SSRF when env injection supplies a non-loopback URL.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_5daf97cf9ab6 -->
- Hermes coord pulse on Windows uses coord_pulse.ps1 plus Task Scheduler OramaCoordPulse every 900s; Tier 0 probe and win_job_queue enqueue then one-shot cursor-agent via win-coder-queue or win-autoresearcher-queue when idle.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_5f151ba1e4c7 -->
- Fan out Win queue work faster with Cursor subagents win-coder-queue and win-autoresearcher-queue in orama-system .cursor/agents; each drains one role at a time via win_job_queue next and complete after peer drop.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_616a871d55ab -->
- P5 implementation ships on cursor/security-pr3-swarm-approval-f559; main stays at planning until PR merge unblocks L1 BLOCKED_PENDING and l1_dispatch API routes.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_62952f6bf927 -->
- P5 T2 api_swarm_preview returns preview_id approval_token expires_at via _sign_swarm_preview and assignments_hash; preview tests need bearer header when auth enforced.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_6406c9e0bb74 -->
- Land P5 swarm HITL on main before any L1 intra-machine dispatch work; L1 reuses the same HMAC preview and launch helpers.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_6b5c074fa6f0 -->
- coord-024 drained orchestrator queue to zero; researcher h4 h5 remain for pulse; triple rinse uses timed coord_pulse and always runs 3x15m listen per outer rinse  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_6cb69214db16 -->
- Operator triple-rinse after coord jobs: push both repos, learn.py batch graduate lessons, auto_dream.py x3, lan_peer_assign drop to Mac, coord_pulse x3, push again  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_704f473c12d5 -->
- Mac portal at 192.168.254.102:8002 can flap (timeout on pulses 1-2) then recover on pulse 3 in the same batch — retry peer drops after portal-health PASS, do not treat transient timeout as Mac offline  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_7312c018843c -->
- 15-minute coord_monitor with Mac peer reachable and win queue 0 pending / added=[] is healthy idle — not a failure; next work is feature-branch P5 T3 or Mac-orchestrated fan-out  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_75c2a10f0dd1 -->
- When Win queue is idle but Mac orchestrator has backlog intel, synthesize win-coder assignment cards with fan-out id and enqueue before coord_pulse can claim work.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_79525c17c233 -->
- coord_monitor 15m ticks should treat Mac peer probe success as heartbeat; merge lessons.jsonl with union not ff-only when Mac lands rows during Win idle.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_79f27891cf52 -->
- active_tilting Win Ollama models must resolve to port 11434 not lm-studio :1234: detect_active_tilting_ip returns the LM Studio endpoint; ModelRegistry._resolve_host must branch on backend=ollama and rebuild http://hostname:port from models.yml.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_7e5f13a8e930 -->
- On Mac peer portal-health timeout during coord monitor, Win continues local queue processing and retries lan_peer_assign drop when probe returns green; do not block coder or autoresearcher on peer outage.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_7e6e54192675 -->
- PT lessons.jsonl rebase conflicts with Mac parallel pushes: union-merge rows by lesson id, fix concatenated candidate JSON, run render_lessons.py, then git rebase --continue — never hand-edit LESSONS.md  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_7f58c5e14499 -->
- PT bridge reconcile complete: subagent/win-coder/bridge-http-local and cursor/review-bridge-http-local-c4ae PT 183 are identical; operator should merge draft PR 183 preferring the cursor review branch; 38/38 tests on http-local preflight.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_83111367be02 -->
- Mac mac-cycle inbox drops are informational; synthesize win-coder assignment cards for win_job_queue pickup  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_8380d37c79f4 -->
- H5 autoresearch GPU harness run_h5_gpu_benchmark.py runs three rubric coding prompts on Win LM Studio 27B with max five iterations each. Drop gpu-results-h5.md to Mac peer inbox. Mac Ollama 9B parallel leg required to close cross-host comparison.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_885af5c81b25 -->
- Co-orchestrator portal monitor URLs: Mac operators use /co-orchestration/macos on port 8002; Win operators use /peer-inbox. Legacy /co-orchestration/windows redirects to /peer-inbox on Win hosts.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_8efcd266b963 -->
- Track P5 swarm HITL progress in docs/plans/P5-STATUS.md with T1-T7 checklist and acceptance criteria; implementation branch cursor/security-pr3-swarm-approval-f559 not yet created on remote.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_90279ae25c6d -->
- Coord job cycle: after each completed Mac inbox or backlog job run learn.py and auto_dream.py, push main, then wait 15 minutes probe and list inbox before taking the next single job; install_coord_pulse.sh provides automated 900s launchd pulse when operator loads it.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_90c360285548 -->
- Post-ship rinse: learn all interaction lessons, auto_dream x3, push both repos, peer drop artifacts, coord_pulse dry-run x3, coord_monitor 15m listen.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_91c7717fd629 -->
- mac_job_queue _priority must use the same bold-tolerant Priority regex as win_job_queue so assignment cards with markdown bold sort correctly.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_94c50ca0b2c1 -->
- Cross-repo Python policy parity scripts must compare function ASTs with ast.dump(), not ast.unparse() — quote-style and formatting differences cause false diverge failures even when logic is identical.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_95e17a736b68 -->
- Work queue job from inbox drops: process one Mac backlog item then one Win assignment per coord round; when subagent branch has zero commits ahead of main the deliverable already merged — skip PR and mark backlog done.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_967706d68c95 -->
- Tests and docs must not embed $HOME/ or other workstation-specific absolute paths — repo_hygiene LINT-006 blocks them; use <local-temp> placeholders or repo-relative paths in fixtures.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_9ba71d7a07ea -->
- L1 local dispatch weekend build reuses P5 HMAC preview tokens and Option C portal wedge; ship ingredients only until sign_operator_payload exists on main.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_9bd695cae223 -->
- H3 autoresearch hypothesis falsified for trivial prompts: Win 27B wall clock about 10 seconds on trivial prompt class versus faster Mac 9B MLX. Route by task class in routing.yml — quality-heavy and autoresearch-coder to win-rtx3080; latency-sensitive probes and orchestrator chatter to Mac Ollama. Affinity is not a universal speed win.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_9c480ce2ff15 -->
- Coord listen longest-wait-wins: job_cycle_listen.sh waits 900s from last_job_finished_at and resets deadline when coord_mark_job_done runs during wait; jobs run immediately when queue actionable.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_9c9c36fd7e3e -->
- LAN co-orchestration cycles honor v1.1 frugality tiers 0-2 local-first per docs/plans/2026-05-29-03-v1.1-definitive.md; state escalation tier in one line; no new architecture beyond existing graceful-degradation ladders.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_9dda7b065806 -->
- Win coord_pulse.ps1 is queue-gated: cursor-agent spawns only when win_job_queue has a non-blocked pending job; Mac coord_pulse.sh when idle still uses a generic mac-orchestrator-queue card to scan inbox and backlog.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_a45dd654f502 -->
- win-coord-listen-022 assignment: Mac orchestrator requests Win run coord_monitor -Minutes 45 (three 15m ticks) in parallel with Mac job_cycle_listen; post ack win-coord-022-mac-gossip-ack.md  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_a55b1287a34d -->
- /autoplan on execution plan produced GSTACK REVIEW REPORT with amendments A1-A6 before coding; lock decisions in P5-DECISIONS-LOCKED.md to prevent silent drift.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_a7593f35a884 -->
- sanitize_tracked_path_leaks must redact workspace tree paths (Downloads/SKILLS.md/ultrathink variants) to <workspace-root> after home-prefix substitution — %USERPROFILE% alone is not sufficient for tracked memory  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_a7c53e96a15c -->
- Before git pull --rebase when Perpetua-Tools main is behind origin, stash local devices.yml and submodule drift; remote may have landed security PR lessons and episodic memory while Mac was idle.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_a9e667a9c61c -->
- POST /runtime/bootstrap logging must pass payloads through redact_runtime_payload before emission — never log raw bootstrap bodies that may contain tokens or secrets.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_aa80b86ee794 -->
- coord triple rinse tick: pulse job done, pushed sync  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_aac9963257dc -->
- win-code-review.md deliverable is acked on Mac; follow-on Win work is playbook doc pass (swarm_state.md ownership) on subagent/win-coder/mac-co-orchestrator-playbook — not re-running full bridge review  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_ac19b529e177 -->
- Coord operator default: both orama-system and Perpetua-Tools on main for rinse/pulse/sync; P5 and other feature work stays on branches until PR merge — do not leave Win session checked out on implementation branch for routine coord  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_b25495a786e7 -->
- When coord queues idle over 15 minutes with no new inbox cards, resume V1_DEFERRED_BACKLOG: operator PRs, P1 frugality_router, and v1.1 plan section 9 out-of-scope items.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_b3a90b3719df -->
- Graceful degradation Ladders A through F in oramasys-method references graceful-degradation.md are SSOT; PT perpetua-model-selection tiers 0-6 map to Ladder B; working cards hold pointers only not duplicate ladder prose.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_bb2471386fe9 -->
- During Win coord listen, Mac may push PT main (lessons.jsonl) — monitor tick git-behind is a signal to git pull --rebase origin main on both repos before next tick  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_bd2dcc808abe -->
- Policy docs that explain forbidden path patterns must use placeholder notation OS-specific home-directory paths or OS-specific home-directory path form — never literal OS-specific home-directory segments or the linter will fail even on warning docs  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_bd406ce3cbb6 -->
- Graceful degradation Ladder F adds model-routing-check dispatch gate: endpoint reachability, hardware_policy_cli affinity fail-closed, routing.yml cross-check, degraded single-endpoint continue per perpetua-model-routing-check.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_bd83f5cdc1de -->
- Triple rinse per Mac coord-023: outer loop 3 times of (coord_pulse + learn.py batch + push both repos), then 3x15m listen windows — not a single pulse triple  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_c126d5ac15c5 -->
- After a PR merges, always verify origin/main contains every commit on the feature branch — unattended merges and continued pushes to the same branch name leave post-merge commits orphaned until a follow-up PR is opened.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_c175581ccd83 -->
- Portal lifecycle mutating routes (stop, restart, rediscover) require same-origin Origin or Referer guards in addition to bearer token — CSRF protection is mandatory; session cookies are optional UX only per SECURITY.md.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_c18da50d3bd5 -->
- Draft PR triage: three-dot diff inflates post-merge orphan PRs; always compare two-dot at branch tip vs main and cherry-pick net-new commits instead of merging stale multi-hundred-commit-behind branches.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_c3afe1820d4a -->
- Portal HTML rendering must escape all user-controlled strings with html.escape (or equivalent _tag helper) before embedding in templates — never interpolate raw query params or flash messages into HTML.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_c55f8d4a751b -->
- After each LAN coord round both Mac and Win run PT learn.py for session lessons and auto_dream.py to stage patterns; commit and push .agent/memory before the next fan-out.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_c89793f9c9ab -->
- H5 cross-host closed coord-005: Mac Ollama 9B 3/3 pass at iterations 1/4/5 (490s wall) versus Win LM Studio 27B 3/3 at 1/1/1 (280s wall). Win wins iterations-to-pass and wall on all rubric tasks. Final synthesis in gpu-results-h5-final.md; routing keeps autoresearch-coder on Win 27B.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_c965a1ce667b -->
- PowerShell ops scripts under orama-system must use ASCII-only punctuation in string literals; Unicode em-dash or smart quotes cause ParserError on Windows PowerShell 5.x and break background monitors like coord_monitor.ps1.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_ca5bfd9ec1c0 -->
- Mac job_cycle_listen.sh resets 15m timer on job_done; Win coord_monitor uses fixed interval without reset-on-job parity gap.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_cd5a32766062 -->
- Reject review-queue candidates that promote personal Downloads or USERPROFILE workspace trees as canonical; use orama-system + Perpetua-Tools repo names and REPO_ROOT env anchors instead  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_d7bb9620f3b2 -->
- Coord cycles 013-015: Mac coord_pulse P2.1 parity (pulse-gate, flock, idle exit) matches Win.ps1; PT #199 frugality_router merged after Win reconcile; triple 15m listen between jobs.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_d7e22a58a28a -->
- coord-023 drained 18 ack queue items; triple rinse loop runs 3 outer cycles of pulse+learn+push then 3x15m listen; queue down to mac-orchestrator-self-improve-003 plus researcher backlog  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_de2ececf24e1 -->
- G1 frugality harness spike is parallel to P5: frugality-report script plus v1_1 session test plan ingredients only until operator approves scope.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_e46007f83d7e -->
- Mac coord pulse uses launchd com.orama.coord-pulse every 900s running coord_pulse.sh: Tier 0 probe and inbox diff when idle, then one-shot cursor-agent via mac-orchestrator-queue.md; OpenClaw Gateway is not the scheduler — file inbox plus win_job_queue on Win remain transport.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_e4a93e148209 -->
- Mac coord_triple_rinse.sh on main: outer loop runs coord_pulse jobs up to MAX_JOBS, then learn+auto_dream+git pull/push on both repos, then job_cycle_listen --rounds 3 (3x15m); Win mirrors with manual pulse batch + coord_monitor  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_e562f896eb7b -->
- At end of each coord monitor window git fetch and pull --rebase origin main on both orama-system and Perpetua-Tools then push before reporting session complete; Win may land lessons and results while Mac monitors.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_e6cd5183f153 -->
- Mac coord_pulse.sh must exit when mac_job_queue is not idle; do not spawn cursor-agent after logging skip-defer or you double-run with manual cycle.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_e723e148aab7 -->
- Coord cycles 010-012: install coord-pulse launchd, mac_job_queue P2 mirror for Mac inbox, merge PT #183 after Win reconcile; 3x15m listen loop between jobs.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_f112b9b9a5f4 -->
- win_job_queue next must skip BLOCKED_PENDING jobs same as coord_pulse.ps1 to avoid claiming L1 before P5  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_f2c50103b15d -->
- PT bridge-http-local Tier 1 draft is PR #183 cursor/review-bridge-http-local-c4ae; win-bridge-pr-ready.md maps to 38/38 tests; Win coder reconciles branch vs draft before operator merge.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_f40725822de5 -->
- When reconciling cross-agent PR branches, diff the bridge files directly not branch tips; identical autoresearch_bridge.py means merge-ready even if commit SHAs differ.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_f6b9a9234e75 -->
- Win LAN peer inbox canonical URL is /peer-inbox served from platform/windows/peer_inbox_portal.py. /co-orchestration/windows and /co-orchestration on Win hosts 307 redirect to /peer-inbox. Hermes co_orchestration_windows skin removed.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_f797e7b5c7eb -->
- PT lessons.jsonl parallel Mac Win pushes cause rebase conflicts; union-merge jsonl lines split concatenated entries then render_lessons.py before rebase --continue.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_f9de2cd2f1e0 -->
- Mac pulse snapshots inbox filenames to last_pulse_seen.json for diff; Win pulse relies on win_job_queue enqueue only without inbox filename diff in the tick script.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_fa13634b4dd4 -->
- coord_monitor.ps1 during active cycles validates lesson_9b3d7e2f41ac: probe plus enqueue detected coord-005 win-autoresearcher-h5-finalize.md at monitor tick 5 before manual operator poll.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_fb7dad1681dc -->
- P5-STATUS on main tracks branch progress without merging code: T1-T2 on cursor/security-pr3-swarm-approval-f559; portal on main still has approved:true until PR merges  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_fbd7259fe844 -->
- mac-orchestrator-self-improve-003 is superseded per mac-self-improve-cycle-003-ack.md — Win reconciles PT branch subagent/mac-orchestrator/self-improve-memory with main landmark note, not new feature work  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_fbd7b42af0bd -->
- When Mac and Win both push Perpetua-Tools .agent/memory in the same coord round, git pull may conflict on lessons.jsonl and LESSONS.md; union-merge all lesson rows from both sides and never drop Mac learn.py rows or Win operator-approve rows.  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_fbdbe7036152 -->
- job_cycle_listen.log and coord_monitor ticks are coord telemetry (sync/probe/gate), not GossipBus mesh transport; v2 gossip uses HTTP tail/ingest of redacted GossipBus events; file inbox stays audit trail  <!-- status=legacy confidence=0.7 evidence=0 id=lesson_legacy_fdc44945483f -->
- Before git pull --rebase when Perpetua-Tools main is behind origin, stash local devices.yml and submodule drift; remote may have landed security PR lessons and episodic memory while Mac was idle.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_98ee8236547d -->
- Fan out Win queue work faster with Cursor subagents win-coder-queue and win-autoresearcher-queue in orama-system .cursor/agents; each drains one role at a time via win_job_queue next and complete after peer drop.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_db15dbdc9000 -->
- After Mac security PR auth hardening peer-file returns HTTP 401 until Win portal restarts with start.ps1 --lan-peer so joint control plane token resolves; retry drops after probe green.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_77688462f96e -->
- Before git pull when Perpetua-Tools is behind origin stash local candidate and submodule drift then pop or checkout --theirs on conflict; Mac may land 20+ learn.py rows while Win idle.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_b545fe9db94c -->
- Work queue job from inbox drops: process one Mac backlog item then one Win assignment per coord round; when subagent branch has zero commits ahead of main the deliverable already merged — skip PR and mark backlog done.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_7f4747e9ae21 -->
- PT bridge-http-local Tier 1 draft is PR #183 cursor/review-bridge-http-local-c4ae; win-bridge-pr-ready.md maps to 38/38 tests; Win coder reconciles branch vs draft before operator merge.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_234b18d4de0c -->
- P1 frugality_router.py spike lands on subagent/mac-orchestrator/frugality-router-spike with resolve_route tier 0-2 first, ORAMASYS_OFFLINE rejects tier>=3, privacy_critical forbids tier>=4, and ot.tool.tier JSONL traces; G1 frugality-report harness remains follow-on per v1.1 plan section 11.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_51978d9dc250 -->
- Win bucket drain ack win-bucket-drain-ack.md signals coord-005 backlog cleared; next Mac job from V1_DEFERRED_BACKLOG is P1 frugality_router then operator PR triage PT 183.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_d2325989d100 -->
- Land P5 swarm HITL on main before any L1 intra-machine dispatch work; L1 reuses the same HMAC preview and launch helpers.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_a5fe55c0e993 -->
- When reconciling cross-agent PR branches, diff the bridge files directly not branch tips; identical autoresearch_bridge.py means merge-ready even if commit SHAs differ.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_575e0a66eeb3 -->
- Defer future autoplan features as win-coder inbox cards with explicit prerequisite gates and low queue priority so coord cycles stay first.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_44c3a074f03c -->
- After each coder queue job completes, run coord_monitor 15 minutes then Mac peer probe, push sync, and batch learn.py before second push.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_41ce7aa5a532 -->
- PT bridge reconcile complete: subagent/win-coder/bridge-http-local and cursor/review-bridge-http-local-c4ae PT 183 are identical; operator should merge draft PR 183 preferring the cursor review branch; 38/38 tests on http-local preflight.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_65cc66d6d356 -->
- PT .agent memory commits belong on main only; subagent PR branches must contain code-only diffs — cherry-pick memory to main and reset spike branch to c7161de before opening PR.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_94a6ff5363ae -->
- Mac coord pulse uses launchd com.orama.coord-pulse every 900s running coord_pulse.sh: Tier 0 probe and inbox diff when idle, then one-shot cursor-agent via mac-orchestrator-queue.md; OpenClaw Gateway is not the scheduler — file inbox plus win_job_queue on Win remain transport.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_5b38450b814c -->
- Coord job cycle: after each completed Mac inbox or backlog job run learn.py and auto_dream.py, push main, then wait 15 minutes probe and list inbox before taking the next single job; install_coord_pulse.sh provides automated 900s launchd pulse when operator loads it.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_7432957e086c -->
- coord_monitor 15m ticks should treat Mac peer probe success as heartbeat; merge lessons.jsonl with union not ff-only when Mac lands rows during Win idle.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_338fa8879fdf -->
- Hermes coord pulse on Windows uses coord_pulse.ps1 plus Task Scheduler OramaCoordPulse every 900s; Tier 0 probe and win_job_queue enqueue then one-shot cursor-agent via win-coder-queue or win-autoresearcher-queue when idle.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_f1de20c86636 -->
- Blocked prerequisite jobs stay in win_job_queue pending but coord_pulse.ps1 skips them via BlockedPending list until operator unblocks.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_361923307118 -->
- Coord cycles 010-012: install coord-pulse launchd, mac_job_queue P2 mirror for Mac inbox, merge PT #183 after Win reconcile; 3x15m listen loop between jobs.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_362521b44592 -->
- Mac mac-cycle inbox drops are informational; synthesize win-coder assignment cards for win_job_queue pickup  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_fc448885c51a -->
- win_job_queue next must skip BLOCKED_PENDING jobs same as coord_pulse.ps1 to avoid claiming L1 before P5  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_3ab1237a6625 -->
- Parse bold markdown Priority fields in assignment cards when computing queue sort order  <!-- status=accepted confidence=0.535 evidence=1 id=lesson_4d3962e3dece -->
- Coord cycles 013-015: Mac coord_pulse P2.1 parity (pulse-gate, flock, idle exit) matches Win.ps1; PT #199 frugality_router merged after Win reconcile; triple 15m listen between jobs.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_7b816eb46b17 -->
- Win coord_pulse.ps1 is queue-gated: cursor-agent spawns only when win_job_queue has a non-blocked pending job; Mac coord_pulse.sh when idle still uses a generic mac-orchestrator-queue card to scan inbox and backlog.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_eaa5e10d7c2c -->
- Win job queue roles are coder and autoresearcher; Mac mac_job_queue roles are orchestrator and researcher mirroring the same one-active-per-role serial pattern.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_8f897af090e2 -->
- Mac coord_pulse.sh must exit when mac_job_queue is not idle; do not spawn cursor-agent after logging skip-defer or you double-run with manual cycle.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_907f55684408 -->
- mac_job_queue _priority must use the same bold-tolerant Priority regex as win_job_queue so assignment cards with markdown bold sort correctly.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_97a37458ba78 -->
- Mac pulse snapshots inbox filenames to last_pulse_seen.json for diff; Win pulse relies on win_job_queue enqueue only without inbox filename diff in the tick script.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_9765a97b0dc4 -->
- When Mac merges a PT PR during Win coord_monitor listen window, pull main immediately and run combined frugality plus bridge test suites before dropping merge-ack to Mac peer.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_396409d55e60 -->
- L1 local dispatch weekend build reuses P5 HMAC preview tokens and Option C portal wedge; ship ingredients only until sign_operator_payload exists on main.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_6739b69bfee8 -->
- L1 killswitch is tiered: POST /api/l1/stop terminates registered child PIDs only; full POST /api/stop NUCLEAR stack runs only on explicit Stop All Emergency control.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_da1d0bdac499 -->
- L1 preview is mandatory for fan-out two or more jobs and all HITL-class launches; single read-only Codex may stay bearer-only fast-path like dispatch_codex_partner.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_45eabd551f44 -->
- When Win queue is idle but Mac orchestrator has backlog intel, synthesize win-coder assignment cards with fan-out id and enqueue before coord_pulse can claim work.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_eeeeca1c78f2 -->
- G1 frugality tier ratio stays not measurable until frugality-report script and v1_1 session harness ship even after frugality_router merges on PT main.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_d3e147880fb7 -->
- Triple rinse cycle: execute queue job, learn from PT agent, auto_dream, push sync, then coord_pulse dry-run when idle repeats Mac Win coord loop.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_1becd8c06b0b -->
- Coord listen longest-wait-wins: job_cycle_listen.sh waits 900s from last_job_finished_at and resets deadline when coord_mark_job_done runs during wait; jobs run immediately when queue actionable.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_94c36b217c90 -->
- Track P5 swarm HITL progress in docs/plans/P5-STATUS.md with T1-T7 checklist and acceptance criteria; implementation branch cursor/security-pr3-swarm-approval-f559 not yet created on remote.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_dec314f0af1c -->
- G1 frugality harness spike is parallel to P5: frugality-report script plus v1_1 session test plan ingredients only until operator approves scope.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_f29979493d7a -->
- Mac job_cycle_listen.sh resets 15m timer on job_done; Win coord_monitor uses fixed interval without reset-on-job parity gap.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_46518e0d3816 -->
- Pulse unified coord-019: both hosts use win_job_queue and mac_job_queue pulse-gate, last_pulse_seen inbox diff, BLOCKED_PENDING in Python only; Win keeps role-specific agent cards and coord_monitor listen; Mac keeps flock lock and job_cycle_listen reset-on-job.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_ea4571839e9f -->
- P5 T1 sign_operator_payload embeds expires_at inside HMAC canonical JSON; verify uses secrets.compare_digest and rejects expired tokens.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_b17b09dbf7c0 -->
- Lock P5 decisions in docs/plans/P5-DECISIONS-LOCKED.md on implementation branch; amend via explicit PR note not silent drift.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_ea2950bef831 -->
- P5 implementation ships on cursor/security-pr3-swarm-approval-f559; main stays at planning until PR merge unblocks L1 BLOCKED_PENDING and l1_dispatch API routes.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_c0caf60f0cb8 -->
- /autoplan on execution plan produced GSTACK REVIEW REPORT with amendments A1-A6 before coding; lock decisions in P5-DECISIONS-LOCKED.md to prevent silent drift.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_46c828cb56e6 -->
- After T1 merge on branch, l1_dispatch _p5_landed import succeeds but CLI still blocked until portal /api/l1/* wired in T2-T7.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_fa0c5c1f8caa -->
- PT lessons.jsonl parallel Mac Win pushes cause rebase conflicts; union-merge jsonl lines split concatenated entries then render_lessons.py before rebase --continue.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_8e3f51cc9de9 -->
- P5 closes client approved bool not human review: HMAC proves preview endpoint ran; stolen bearer can still auto preview-launch within TTL.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_44b19fa16e6b -->
- Win queue idle after job burst is expected; synthesize win-coder cards from Mac inbox intel when orchestrator has backlog.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_6a40e7cdcc09 -->
- Post-ship rinse: learn all interaction lessons, auto_dream x3, push both repos, peer drop artifacts, coord_pulse dry-run x3, coord_monitor 15m listen.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_a5d22ead6088 -->
- P5 T2 next: api_swarm_preview returns preview_id approval_token expires_at signed via sign_operator_payload with assignments_hash in canonical payload.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_aa77d4508b52 -->
- P5 T2 api_swarm_preview returns preview_id approval_token expires_at via _sign_swarm_preview and assignments_hash; preview tests need bearer header when auth enforced.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_f3147e01a67d -->
- job_cycle_listen.log and coord_monitor ticks are coord telemetry (sync/probe/gate), not GossipBus mesh transport; v2 gossip uses HTTP tail/ingest of redacted GossipBus events; file inbox stays audit trail  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_eecb0f71cc4f -->
- Anti-doxxing must be wired at every layer: protocols/path-hygiene.md, AGENTS.md, permissions.md never-rule, path_hygiene.py sanitize, Cursor alwaysApply rules in PT and orama, and repo_hygiene CI — not just LESSONS bullets  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_37e04950f3fe -->
- sanitize_tracked_path_leaks must redact workspace tree paths (Downloads/SKILLS.md/ultrathink variants) to <workspace-root> after home-prefix substitution — %USERPROFILE% alone is not sufficient for tracked memory  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_992cafaca36b -->
- Reject review-queue candidates that promote personal Downloads or USERPROFILE workspace trees as canonical; use orama-system + Perpetua-Tools repo names and REPO_ROOT env anchors instead  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_324689a515a7 -->
- ~~Policy docs that explain forbidden path patterns must use placeholder notation OS-specific home-directory paths or OS-specific home-directory path form — never literal OS-specific home-directory segments or the linter will fail even on warning docs~~  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_889a1c180031 superseded_by=lesson_460ac276e494 -->
- 15-minute coord_monitor with Mac peer reachable and win queue 0 pending / added=[] is healthy idle — not a failure; next work is feature-branch P5 T3 or Mac-orchestrated fan-out  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_3a690b020b00 -->
- P5 swarm HITL on cursor/security-pr3-swarm-approval-f559: T1 operator payload + T2 preview signing done (2/7); T3 launch token verify is next; L1 /api/l1/* stays blocked until PR merges  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_4ad1b44cc918 -->
- PT lessons.jsonl rebase conflicts with Mac parallel pushes: union-merge rows by lesson id, fix concatenated candidate JSON, run render_lessons.py, then git rebase --continue — never hand-edit LESSONS.md  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_357eb41fde9b -->
- Operator triple-rinse after coord jobs: push both repos, learn.py batch graduate lessons, auto_dream.py x3, lan_peer_assign drop to Mac, coord_pulse x3, push again  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_a8bcd373c9bd -->
- coord-023 drained 18 ack queue items; triple rinse loop runs 3 outer cycles of pulse+learn+push then 3x15m listen; queue down to mac-orchestrator-self-improve-003 plus researcher backlog  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_83b6abe6f405 -->
- Mac portal at 192.168.254.102:8002 can flap (timeout on pulses 1-2) then recover on pulse 3 in the same batch — retry peer drops after portal-health PASS, do not treat transient timeout as Mac offline  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_5baad6942a2a -->
- win-coord-listen-022 assignment: Mac orchestrator requests Win run coord_monitor -Minutes 45 (three 15m ticks) in parallel with Mac job_cycle_listen; post ack win-coord-022-mac-gossip-ack.md  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_368e6f78d629 -->
- coord_pulse returns idle when win_job_queue pending=0 even if lan_peer inbox has new markdown cards — inbox files are not jobs until enqueued or pulse-gate marks them actionable  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_636dc1bd72d8 -->
- During Win coord listen, Mac may push PT main (lessons.jsonl) — monitor tick git-behind is a signal to git pull --rebase origin main on both repos before next tick  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_fd667ab40efb -->
- Triple rinse per Mac coord-023: outer loop 3 times of (coord_pulse + learn.py batch + push both repos), then 3x15m listen windows — not a single pulse triple  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_49989c3c0968 -->
- Coord operator default: both orama-system and Perpetua-Tools on main for rinse/pulse/sync; P5 and other feature work stays on branches until PR merge — do not leave Win session checked out on implementation branch for routine coord  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_538699df38b4 -->
- Mac coord_triple_rinse.sh on main: outer loop runs coord_pulse jobs up to MAX_JOBS, then learn+auto_dream+git pull/push on both repos, then job_cycle_listen --rounds 3 (3x15m); Win mirrors with manual pulse batch + coord_monitor  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_5ae30a69d6b9 -->
- mac-coord-023-queue-ack reconciled 18 Win inbox drops on Mac; remaining Mac work is mac-orchestrator-self-improve-003 and researcher GPU backlog — Win queue empty is expected post-drain  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_fec955e347c5 -->
- P5-STATUS on main tracks branch progress without merging code: T1-T2 on cursor/security-pr3-swarm-approval-f559; portal on main still has approved:true until PR merges  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_ed0ef7c21784 -->
- coord-024 drained orchestrator queue to zero; researcher h4 h5 remain for pulse; triple rinse uses timed coord_pulse and always runs 3x15m listen per outer rinse  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_1eb60ac2018e -->
- coord triple rinse tick: pulse job done, pushed sync  <!-- status=accepted confidence=0.36 evidence=1 id=lesson_497b441557d4 -->
- mac-orchestrator-self-improve-003 is superseded per mac-self-improve-cycle-003-ack.md — Win reconciles PT branch subagent/mac-orchestrator/self-improve-memory with main landmark note, not new feature work  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_6c8aed1a143d -->
- win-code-review.md deliverable is acked on Mac; follow-on Win work is playbook doc pass (swarm_state.md ownership) on subagent/win-coder/mac-co-orchestrator-playbook — not re-running full bridge review  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_723cc8acbe94 -->
- When Win queue is drained post coord-023, operator must fan out new win-coder-* and win-autoresearcher-* assignment cards to local inbox and win_job_queue.py enqueue before pulse can dispatch jobs  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_7be44425b675 -->
- Researcher GPU backlog after H5: next cycle is preflight/doc spike (h6) on subagent/win-autoresearcher/researcher-backlog-h6 unless Mac drops new hypothesis card  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_0bf04410f4b3 -->
- Researcher backlog handoff after H5: Win autoresearcher completes doc-spike preflight (gpu-results-h6-preflight.md), drops to Mac peer, and blocks next GPU harness until Mac researcher fans out a new hypothesis card with falsification criteria  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_599216f1810f -->
- portal-health PASS + peer-lmstudio PASS does NOT mean dispatch authorized — POST to /api/peer-file returns HTTP 401 without openclaw.gateway-auth-token in Keychain (portal-status SKIP = token absent). Always check auth layer separately from reachability layer.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_07c8bb2e3197 -->
- New markdown files over 200 lines trigger a WARNING (not a block) from orama repo_hygiene pre-commit — commit still succeeds. Consider offloading details to references/ or sub-skills for files approaching this size.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_01688d19ce4f -->
- Cross-harness wiring docs belong in orama-system/docs/how-to/ — not inside skill references/ directories (those are skill-internal) and not in docs/wiki/ (wiki is concept-level). how-to/ is for operational runbooks agents and humans self-reference.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_bc102bd9e95a -->
- openclaw.json's .cron.jobs schema is NOT the raw shape shown in some skill docs (id/agentId/schedule.type/schedule.value/prompt) — hand-editing that shape breaks config validation silently until 'openclaw config validate' or 'openclaw status' surfaces 'cron: Invalid input'. Always use 'openclaw cron add --agent X --every/--cron/--at ... --message ...' CLI instead of jq-editing openclaw.json directly.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_67ddcb4837f2 -->
- This machine's OpenClaw launchd label is ai.openclaw.gateway (not com.openclaw.gateway as some skill docs assume) on port 18789 (not 7331), and exposes a WebSocket gateway (ws://) not a plain HTTP /health route — curl to /health will always fail there. Use 'openclaw status' or 'openclaw gateway probe' to check liveness, not curl.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_681ae028587d -->
