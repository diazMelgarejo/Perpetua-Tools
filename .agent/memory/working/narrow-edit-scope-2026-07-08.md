# Narrow Edit Scope

Date: 2026-07-08

Edit only the files and lines the user names. Do not clean up or improve adjacent code without asking first.

A nearby line is not in scope just because it is related. Preserve unrelated code byte-for-byte and verify the final diff contains only requested hunks.
