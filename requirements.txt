# Compatibility entrypoint for ``pip install -r requirements.txt``.
#
# ``pyproject.toml`` is the only dependency declaration.  Keeping a second
# package list here previously allowed bootstrap and CI to drift from the
# application contract.  This intentionally carries no version pins.
-e .
